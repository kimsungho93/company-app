# 로그인 캠퍼스 시각 품질 개선

## 범위와 책임

사용자가 지적한 미니어처 느낌을 줄인다. 낮은 원근 카메라, 자연광과 반사, 건물 패널·도로 질감·가지와 잎·사람 비율·자동차 곡면을 개선한다. 건물 중앙에는 공식 SK hynix CI를 표시하고 중간 띠는 SK 레드로 변경한다. 좌측 하단은 요청한 표기 `SKHNIX 2 CAMPUS`를 사용한다. 기존 IBS 서비스 브랜드와 인증 폼은 유지한다.

시작 상태 341개 파일을 `company-backend/build/login-campus-realism-stage/before`와 `baseline.json`에 보존했다. 기존 관리자·하네스 변경을 덮어쓰지 않는다. [이전 캠퍼스 구현과 미검증 기록](docs/plans/2026-09-17-campus-initial.md)은 보존한다. 커밋·푸시는 범위 밖이다.

설치된 Three.js와 부가 모듈을 재사용한다. 환경과 이동 객체는 별도 파일에서 생성하고 렌더러가 광원·카메라·수명주기를 관리한다. 공식 CI만 로컬 자산으로 추가하고 외부 런타임 요청은 만들지 않는다. 브라우저용 절차적 모델의 품질 한계가 있으며 언리얼 엔진 수준의 실사 품질을 완료 조건으로 주장하지 않는다.

## 완료 조건

| ID  | 구분 | 결과                                                                                   | 검증                                         | 상태                                 |
| --- | ---- | -------------------------------------------------------------------------------------- | -------------------------------------------- | ------------------------------------ |
| R-1 | 필수 | 원근과 재질·지형·수목·인물·차량이 기존 단순 모델보다 구체적으로 표현된다               | 데스크톱·모바일 실제 Chromium 캡처 비교      | 통과 — 최종 캡처 비교                |
| R-2 | 필수 | 건물 중앙 공식 CI, 붉은 층 띠, 정확한 하단 문구                                        | 공식 출처 대조·캡처                          | 통과 — 공식 원본·캡처 대조           |
| R-3 | 필수 | 사람의 게이트 통과와 양방향 차량 이동, 정지·동작 줄이기·실패 대체·반응형 폼이 유지된다 | 관련 단위 테스트·기존 브라우저 smoke         | 통과 — 관련 18건·최종 브라우저 5건   |
| R-4 | 필수 | 새 자산과 GPU 자원 수명주기, 타입·스타일·계층 검사 및 기존 변경 보존                   | 관련 검사·독립 읽기 전용 리뷰·파일 해시 대조 | 통과 — 검사·리뷰·실제 반영 해시 대조 |
| R-5 | 권고 | 저사양 실제 기기 성능과 실제 비활성 탭 전환                                            | 대상 기기 관찰                               | 미검증 — 이전 환경 제약 유지         |

## 검증 기록

- 2026-09-17~18 `yarn verify`: 포맷·lint·구조 검사, 구조 도구 7건·평가 입력 5건·Vitest 553건·타입/배포 빌드 통과. 로컬 근거 `company-backend/build/login-campus-realism-stage/verify.log`.
- 이후 카메라 경계 테스트 6건을 추가하고 관련 모션 9건과 함께 15건 통과했다. 원근 카메라의 넓은 화면에서도 사람·차량 반복 지점이 화면 밖이고 정문·CI가 보이는지 검사한다.
- 실제 Chromium에서 1600×1000·820×1000·390×844·3440×900 화면을 캡처했다. API는 mock이며 실제 백엔드 인증·쿠키 통합 검증이 아니다.
- 초기 브라우저 검사에서 렌더링 부하로 시간 제한을 넘었다. 검사 제한을 늘리지 않고 차량·인물 메시를 236→132개, 삼각형을 약 19만→86,536개로 줄였다(게이트 제외). 그림자와 DPR을 제한하고 실제 렌더링 소요 시간에 따라 30fps 이하로 빈도를 낮춰 입력 처리 여유를 둔다. 프레임이 느려져도 경로는 경과 시간으로 계산한다.
- 독립 초기 리뷰의 두 지적을 수용했다. 내부 보행 반복 지점을 불투명 뒷벽 뒤 z=-4.4로 옮기고 회귀 조건을 강화했다. 태블릿에서 나무와 겹치던 소개 문구를 숨겼다.
- GPU 환경맵의 렌더 타깃, 공식 CI 로딩 콜백을 포함해 해제한다. 신규 의존성·외부 런타임 요청은 추가하지 않았다. 공식 CI 원본과 출처는 `src/features/auth/ui/campus/assets/`에 보존했다.
- [Three.js PMREM 공식 문서](https://threejs.org/docs/pages/PMREMGenerator.html)와 설치 버전의 Sky·재질·그림자 구현을 확인했다. 환경광은 초기 1회 만들고 재사용한다.
- 남은 범위: 실제 저사양 모바일·Safari 성능, 실제 비활성 탭 전환은 미검증이다. 이전 계획의 미검증 기록을 보존한다. 500kB 번들 경고는 남으며 새 캠퍼스 코드는 지연 청크로 분리한다.

- GPU 명령이 누적되는 병목은 WebGL2 fence를 0 timeout으로 확인해 이전 프레임 완료 후 다음 프레임을 제출하도록 수정했다. [clientWaitSync 문서](https://developer.mozilla.org/en-US/docs/Web/API/WebGL2RenderingContext/clientWaitSync)를 확인했다. GPU 동기화 객체도 해제한다.
- 재리뷰에서 긴 정지 시간이 GPU 비용으로 계산되는 지적을 수용했다. 정지와 재시작 모두에서 비용 측정을 초기화하고 미완료 GPU fence 자체는 유지한다. 관련 테스트는 모션 9·카메라 6·프레임 비용 3건, 총 18건 통과했다. 재리뷰의 마지막 reset 변경은 설명 기반 평가였으며 주 에이전트가 원문과 실행 결과를 확인했다.
- 최종 화면 캡처는 로컬 `company-backend/build/login-campus-realism-stage/captures/`에 있다. 최적화 후 데스크톱·태블릿·모바일·회원가입·초광폭을 다시 확인했고 페이지 오류 0·가로 넘침 없음이다.

- 최종 `yarn playwright test e2e/campus.spec.ts`: 5건 통과, 종료 코드 0 (26.9초). `browser-campus-final.log`가 근거다. 이전 기본 smoke의 나머지 4건도 통과했다. 검사 시간 제한이나 검증 조건을 낮추지 않았다.

- 실제 company-app에 이번 변경 22개 경로를 반영하고 모든 반영 파일의 바이트 해시를 확인했다. 실제 저장소의 `yarn verify`도 종료 코드 0으로 통과했다(단위·컴포넌트 562건, 구조 도구 7건, 평가 입력 5건, 포맷·lint·경계·타입/배포 빌드). 근거는 `company-backend/build/login-campus-realism-stage/verify-frontend.log`다. 기존 관리자·하네스 변경과 의존성은 보존했으며 커밋·푸시는 하지 않았다.
- R-1~R-4를 충족했다. R-5의 권고 미검증과 이전 계획의 남은 검증은 위 기록대로 유지한다.
