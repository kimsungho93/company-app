# 프런트 하네스와 코드 스타일

## 범위

기존 컴포넌트·API 동작을 유지하면서 Oxfmt, 공통 검증 명령, 모듈 경계 검사, 최소 브라우저 테스트, 리뷰·검증 스킬과 평가 사례를 연결한다.

- 기준: `main`, `d017499026195fcae38292e30b54059bafc33865`, 작업 시작 파일 314개의 해시와 사본을 로컬 `company-backend/build/frontend-harness-stage/`에 보존했다.
- 기존 관리자 화면 4개 파일과 `.oxlintrc.json`, Claude 규칙·훅·설정, CI의 미커밋 변경을 유지한다. 필요한 설정 병합과 자동 포맷 외의 기존 업무 코드 수정은 범위 밖이다.
- 새 의존성은 Oxfmt와 Playwright 개발 도구로 제한한다. 기존 Yarn·Oxlint·TypeScript·Vitest를 유지한다.
- 커밋·푸시·원격 브랜치 보호 변경은 수행하지 않는다. 원격 필수 검사 설정과 로컬 CI 구성의 보장을 구분해 보고한다.

## 완료 조건

| ID   | 구분 | 관찰할 결과                                                                            | 검증 방법                                                           | 상태·근거                                                                                |
| ---- | ---- | -------------------------------------------------------------------------------------- | ------------------------------------------------------------------- | ---------------------------------------------------------------------------------------- |
| AC-1 | 필수 | 스타일 자동 적용·검사와 하나의 공통 검증 명령이 있으며 CI가 실제 배포 빌드를 포함한다. | 고정 버전 설치, format 재검사, lint·테스트·build 실행, CI 정적 확인 | 통과 — 실제 저장소 `verify:all` 성공, CI 구성 확인·원격 실행 미실시                      |
| AC-2 | 필수 | 정상 import를 허용하고 alias·상대경로의 계층 역참조와 feature 내부 참조를 거절한다.    | 검사 도구 대조 테스트와 실제 src 검사                               | 통과 — 도구 7건과 실제 src 검사 성공                                                     |
| AC-3 | 필수 | 핵심 사용자 동작을 독립된 실제 Chromium에서 확인하고 mock과 실제 서버 검증을 구분한다. | 로그인·대화상자·화면 크기 등 브라우저 smoke 실행                    | 통과 — Chromium 4건, REST는 mock                                                         |
| AC-4 | 필수 | 간결한 리뷰·검증 스킬, 조건별 PLAN 평가와 하네스 고정 사례를 재사용할 수 있다.         | 스킬 형식·링크 검사, 사례 입력 분리 테스트, 새 문맥 읽기 전용 평가  | 통과 — 스킬 2개·도구 5건·의미 평가 6건, [평가 결과](evals/harness/results/2026-09-17.md) |
| AC-5 | 필수 | 기존 변경을 보존하고 포맷으로 업무 동작을 바꾸지 않는다.                               | 시작 파일 대조, 포맷 전후 구문 비교, 전체 검증, 독립 리뷰           | 통과 — 실행 구문 214개·SCSS 46개 동일, 실제 반영 대조·전체 검증 성공                     |

## 실행과 결정

1. 작업 시작 사본을 만들고 역할별 구현을 분리한다. 실제 프런트는 현재 실행 환경의 쓰기 허용 범위 밖이므로 검토 가능한 사본을 만든 뒤 반영 권한을 요청한다.
2. Oxfmt는 TSX·SCSS 등을 지원하며 현재 Oxlint를 유지할 수 있다. Prettier도 대안이나 현재 별도 플러그인 요구가 없다. 2칸·작은따옴표·세미콜론 생략·100자 목표·LF를 사용하고 import 정렬은 켜지 않는다. 적용 전후 동작 보존을 검사한다.
3. 기존 jsdom 테스트를 유지하고 실제 브라우저에서만 확인할 동작을 Playwright로 추가한다. 브라우저 설치·운영 비용을 고려해 Chromium 소수 흐름으로 시작한다. mock REST는 실제 쿠키·서버 권한·STOMP 통합 검증을 대체하지 않는다.
4. 경계 검사는 이미 설치된 TypeScript의 구문·경로 해석 기능을 재사용한다. 정규식만으로 상대경로를 검사하거나 별도 구조 프레임워크를 도입하지 않는다.
5. 관련 검증 후 독립 리뷰 1회, 수정이 있으면 재리뷰 1회. 필수 조건의 실패·미검증을 점수로 상쇄하지 않는다.

## 인수인계

- 현재: 2026-09-17 실제 `company-app`에 반영 완료. 기존 314파일을 시작 해시와 대조한 뒤 변경 180파일만 복사·바이트 대조했으며 파일 삭제는 없었다. 이 중 기존 src 148파일은 자동 포맷만 변경했다. 포맷 전후 TS/TSX 214파일의 실행 구문과 SCSS 46파일의 컴파일 결과가 동일했다.
- 실행: 실제 프런트 루트에서 `node .yarn/releases/yarn-4.18.0.cjs install --immutable`, `node .yarn/releases/yarn-4.18.0.cjs verify:all`이 종료 코드 0으로 성공했다. 포맷·린트, Vitest 62파일·544건, 경계 검사기 7건과 실제 src, 하네스 입력 도구 5건, 타입·배포 빌드, Chromium 4건을 확인했다. `git diff --check`도 통과했다. 로컬 실행 로그는 `company-backend/build/frontend-harness-stage/verify-frontend.log`에 있다.
- 결정: 개발 서버 HMR 간섭은 빌드 결과 preview로 해결했다. Dialog의 Shift+Tab 순환 가정은 표준과 실제 브라우저에 맞게 고쳤으며 내부 순서·배경 미포커스·닫기/복귀 검증은 유지했다. [기술 결정](docs/adr/0001-frontend-harness.md)에 근거를 남겼다.
- 리뷰: 새 문맥의 읽기 전용 리뷰에서 P1/P2 발견 사항 없음. 축약 문서에서 빠진 것으로 의심한 인증 재발급 예외는 기존 `docs/api/auth.md`와 연결 규칙에 유지되어 있어 수정하지 않았다. 추가로 의미 평가 기록의 조건·필수/금지 기준·입력 해시를 독립 대조해 AC-4 근거를 확인했다.
- 한계: 브라우저 smoke의 REST는 mock이며 실제 서버 권한·쿠키·STOMP를 검증하지 않았다. 원격 CI 실행·브랜치 보호, 커밋·푸시는 수행하지 않았다. 500kB 초과 빌드 청크 경고는 남아 있으며 경고 기준은 낮추지 않았다.
- 다음: 이번 로컬 도입 범위에서 남은 필수 작업 없음. 이후 커밋·푸시나 원격 필수 검사 설정은 사용자 요청 범위를 따른다.
- 최종 판정: 필수 조건 5개 통과, 로컬 도입 완료.
