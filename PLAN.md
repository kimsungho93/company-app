# 웨이퍼 로그인 화면 복원

## 범위

사용자 요청에 따라 캠퍼스 도입 직전 `01cd833`의 웨이퍼와 IBS 노광 애니메이션, 인증 화면 배치를 복원한다. 기준 HEAD는 `3bb0427`이며 시작 시 프론트·백엔드 작업 트리는 깨끗했다. 로그인·회원가입은 같은 AuthLayout을 사용하므로 두 화면의 배경을 함께 복원하고 인증·세션·게임·공통 테마의 나머지 동작은 보존한다.

캠퍼스 전용 구현·자산·테스트·지침을 제거하고 웨이퍼 컴포넌트·폰트 준비 훅·배경 토큰·관련 문서를 복원한다. 캠퍼스가 삭제되므로 WebGL 전용 검사는 웨이퍼의 실제 그리기·동작 줄이기·인트로·공유 캔버스·실패 시 폼 유지 검사로 대체한다. 기존 인증·모바일 smoke는 유지한다. 외부 의존성과 API는 변경하지 않는다. 커밋·푸시는 이번 요청 범위 밖이다.

이전 작업 기록은 [차량 개선 계획](docs/plans/2026-09-18-reference-cars.md)에 보존한다. 시작 파일의 해시는 로컬 `company-backend/build/wafer-restore-stage/baseline.json`에 있다.

## 완료 조건

| ID  | 구분 | 결과                                                       | 검증                                      | 상태 |
| --- | ---- | ---------------------------------------------------------- | ----------------------------------------- | ---- |
| W-1 | 필수 | 기존 웨이퍼·IBS 노광과 다크 화면 배치 복원                 | 이전 소스 대조, 데스크톱·모바일 실제 화면 | 통과 |
| W-2 | 필수 | 로그인·회원가입·동작 줄이기·캔버스 공유 및 실패 시 폼 유지 | 관련 브라우저 검사·인증 테스트            | 통과 |
| W-3 | 필수 | 캠퍼스 제거와 문서 정합성, 스타일·타입·구조 검사           | 사용처 검색·포맷·lint·빌드·경계 검사·리뷰 | 통과 |
| W-4 | 필수 | 검증한 변경만 실제 프론트에 반영                           | 기준 해시와 적용 해시, 범위 외 diff 대조  | 통과 |

## 결과와 남은 범위

- W-1: 런타임 파일을 `01cd833`과 대조했다. Chromium의 1280×720 데스크톱 및 390×660 모바일 캡처에서 IBS 웨이퍼·다크 폼 배치를 확인했다.
- W-2: 인증 단위·컴포넌트 검사 10파일 56건 통과. 전체 브라우저 검사 8건 통과 후 리뷰로 보완한 모바일 키보드 검사 1건도 통과했다. API는 mock이므로 실제 백엔드 연동 검증은 아니다.
- W-3: lint·format:check·check:architecture·경계 검사기 테스트 7건·타입 검사와 빌드 통과. 기존 큰 번들 경고는 남아 있다. 독립 리뷰 1회에서 기존 회원가입 키보드 검사의 보존 의견을 수용하고, 보완 후 재리뷰에서 해결을 확인했다. 캠퍼스 전용 WebGL 검사만 제거하고 폼 동작 검사는 유지했다.
- 모바일 전체 페이지 캡처를 동작 검사 앞에 추가했을 때 포인터별 글꼴 검사에 실패했다. 캡처를 모든 키보드 검사 뒤로 옮겨 검사 환경 간섭을 분리했으며 16px 검사를 포함한 기존 단언은 유지했다.
- W-4: 실제 프론트에 14개 파일 반영·20개 캠퍼스 파일 제거 후 후보와 해시가 일치함을 확인했다. 범위 밖 기준 파일 326개도 원래 해시를 유지한다.
- 브라우저 검증은 Chromium 기준이며 실제 모바일 기기와 운영 서버 연동은 실행하지 않았다. 인증·API·세션 로직과 백엔드는 변경하지 않았다.
