# 참고 사진에 맞춘 캠퍼스 차량 개선

## 범위와 선택

회색 세단과 흰색 SUV 사진을 기준으로 로그인 캠퍼스 차량의 형태·재질·디테일을 개선한다. 차체 곡면, 열린 휠하우스, 구분된 창문과 도장 루프, 그릴·프로젝터 전조등·휠을 조립하고 두 차종을 양방향 도로에 배치한다. 사진에 없는 면은 입체 모델로 구성하며 사진과 동일한 제품 모델이라고 주장하지 않는다.

기준 커밋은 `41a1d3d`다. 시작 시 `AuthLayout.tsx`, `AuthLayout.module.scss`, `campusActors.ts`가 수정돼 있었다. 기존 IBS 로고 복원, WELCOME 문구 제거, 자동차 속도 `12.2 / 10.8`을 보존한다. 351개 파일의 사본과 SHA-256을 로컬 `company-backend/build/reference-cars-stage/before`와 `baseline.json`에 보존했다. [이전 캠퍼스 검증 기록](docs/plans/2026-09-18-campus-realism.md)을 별도 보존한다.

현재 설치된 Three.js와 재질·기하 병합 기능을 사용한다. 사진 스프라이트는 현재 원근 시점과 이동 중 투영 변화에 맞지 않고 바퀴를 독립 회전할 수 없다. 영상은 자동차 속도만 조절하기 어렵다. 따라서 실시간 입체 모델을 유지한다. 신규 주요 의존성·외부 자산 요청·백엔드 및 인증 계약 변경·커밋·푸시는 범위 밖이다.

차량 형상은 `campusCarModels`, 기존 사람 형상은 `campusActorModels`, 색상·재질·동선과 바퀴 회전은 `campusActors`, 광원과 GPU 자원 수명주기는 `campusRenderer`가 맡는다. 자동차 세부 품질을 높이되 정적 부품은 재질별로 병합하고 반사 환경은 초기 1회만 생성한다.

## 완료 조건

| ID  | 구분 | 관찰할 결과                                                                           | 검증 방법                                                           | 상태                             |
| --- | ---- | ------------------------------------------------------------------------------------- | ------------------------------------------------------------------- | -------------------------------- |
| C-1 | 필수 | 회색의 낮은 세단과 흰색의 높은 SUV가 사진의 비율·유리·범퍼·휠 특징을 구분해 표현한다. | 두 차종 근접 이미지와 실제 로그인 데스크톱·모바일 화면 확인         | 통과 — 확대·통합 화면 확인       |
| C-2 | 필수 | 4대가 기존 차선·빠른 속도로 이동하고 바퀴 회전·일시정지·동작 줄이기를 유지한다.       | 이동·카메라 기존 테스트와 Chromium 캠퍼스 smoke                     | 통과 — 관련 18건·브라우저 5건    |
| C-3 | 필수 | 초기화·해제와 로그인 입력을 유지하고 모델 비용을 확인한다.                            | 형상 수치·전후 메시/삼각형 비교, 브라우저 오류/입력 확인, 독립 리뷰 | 통과 — 수치·브라우저·독립 리뷰   |
| C-4 | 필수 | 기존 로고·문구·속도 변경을 보존하고 빌드·스타일·경계 검사를 통과한다.                 | 시작 사본과 diff, 관련 검사 및 실제 반영 파일 해시 대조             | 통과 — 실제 반영 파일 해시 일치  |
| C-5 | 권고 | 실제 저사양 모바일·Safari에서도 성능이 적절하다.                                      | 대상 기기 관찰                                                      | 미검증 — 현재 테스트 환경에 없음 |

## 진행과 근거

- 2026-09-18 실제 Chromium에서 두 차종의 전면·후면 확대 화면과 로그인 데스크톱·390px 모바일 화면을 확인했다. 창틀 돌출과 전조등·후미등 매립을 수정했다. 인증 폼과 기존 로고·문구·주행 속도를 유지했다.
- `yarn vitest run src/features/auth/ui/campus/campusMotion.test.ts src/features/auth/ui/campus/campusCamera.test.ts src/features/auth/ui/campus/campusFramePacing.test.ts`: 18건 통과. `check:architecture`와 검사기 7건, `lint`, `format:check` 310개 파일, 타입 검사와 배포 빌드 통과.
- 후미등·임시 반사 장면 해제 수정까지 포함한 최종 배포 빌드로 `yarn playwright test e2e/campus.spec.ts` 5건 통과, 종료 코드 0을 확인했다(29.9초). 움직임·정지, 동작 줄이기, WebGL 생성 실패/컨텍스트 손실 시 로그인 유지, 모바일 키보드·화면 밖 렌더링 정지를 포함한다. API는 mock이며 실제 서버 인증 통합 검증이 아니다.
- 초기 전체 캠퍼스 반사 캡처는 장면 준비 시간 제한을 넘겼다. 저비용 `RoomEnvironment` 기반 128px PMREM을 초기 1회 생성하는 방식으로 바꾼 후 동일 검사 5건과 종료 코드 0을 확인했다(32.6초). 검사 조건·시간 제한은 변경하지 않았다. 샌드박스에서 테스트 서버 종료가 멈춰 최종 검사는 허용된 샌드박스 밖에서 실행했다.
- 최종 형상은 세단 17,168개·SUV 18,332개 삼각형, 차량당 메시 15개다. 사람·게이트·차량 합계는 메시 138→142개, 삼각형 86,608→108,056개, 지오메트리 배열 9.20→11.16MiB다. 추가 메시 4개는 접지 그림자이며 비정상 수치 0개다. 0.1초 간 이동량은 기존과 같은 +1.22/-1.08이다. 이 수치는 FPS나 실제 기기 메모리 측정을 대신하지 않는다.
- 새 문맥의 `frontend-review`가 코드와 기존 변경 보존을 검토했다. 임시 반사 장면의 `InstancedMesh`도 즉시 해제하자는 의견을 수용했고 후미등 보정과 함께 재검토했다. 조치가 필요한 결함은 없었다. 하늘·차량 환경맵은 렌더 타깃이 소유하며 재질 순회에서 중복 해제하지 않는다.
- 실제 저사양 모바일·Safari 성능은 미검증이다. 기존 500kB 번들 경고는 남고 캠퍼스 렌더러는 지연 로딩한다. 신규 의존성·외부 자산·백엔드 변경·커밋·푸시는 없다.

- 실제 company-app에 범위 내 7개 파일을 반영하고 바이트 해시 일치를 확인했다. 기존 AuthLayout 두 파일은 시작 시점의 해시를 보존했다.
