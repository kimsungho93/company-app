# 앱 셸 (헤더 · 테마 · 승인 관리) 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 인증된 화면을 감싸는 앱 셸(헤더 + 레이아웃)을 만들고, 다크·라이트 테마 전환과 관리자 승인 관리 화면을 붙인다.

**Architecture:** `AppLayout`(app 레이어)이 `AppHeader`(widgets 레이어 신설)를 소유하고 페이지는 `<Outlet />` 에 들어간다. 헤더 조각들은 props 를 받지 않고 각자 `useMeQuery()` 를 구독한다. 테마는 `<html data-theme>` 속성이 진실이며 `useSyncExternalStore` 로 구독한다 — Redux 에 넣지 않는다.

**Tech Stack:** React 19 · TypeScript 6 · Vite 8 · RTK Query · react-router 8 · SCSS Modules · Vitest · oxlint

**Spec:** [docs/superpowers/specs/2026-08-17-app-shell-design.md](../specs/2026-08-17-app-shell-design.md)

## Global Constraints

- **패키지 매니저는 yarn.** `npm install` 금지 — `package-lock.json` 이 생겨 `yarn.lock` 과 어긋난다
- **함수는 화살표 표현식만.** `func-style: ["error", "expression"]` 로 린트에서 막힌다. `export const x = () => {}`
- **타입 전용 import 는 `import type`.** `verbatimModuleSyntax` 가 켜져 있다
- **안 쓰는 변수·파라미터는 빌드 실패.** `noUnusedLocals` / `noUnusedParameters`
- **`enum` 금지.** `erasableSyntaxOnly` — 유니온 타입 리터럴을 쓴다
- **SCSS 는 `@use` / `@forward` 만.** `@import` 금지. 슬라이스에서는 `@use '@/shared/styles' as s;`
- **색을 하드코딩하지 않는다.** `_tokens.scss` 의 CSS 커스텀 프로퍼티만 쓴다
- **`@/` alias 는 두 곳에 있다** — `vite.config.ts` 의 `resolve.alias` 와 `tsconfig.app.json` 의 `paths`. 한쪽만 고치면 어긋난다 (이번 계획에서 바꿀 일은 없다)
- **feature 외부에서는 `features/<name>/index.ts` 가 export 한 것만 쓴다.** 내부 경로 직접 import 금지
- **의존 방향:** `app → pages → widgets → features → shared`
- 검증 명령: `yarn lint` · `yarn tsc -b` · `yarn test` · `yarn build`

---

### Task 1: 토큰을 두 벌로 — 이름을 역할 기반으로 바꾼다

CSS 만 바뀌는 작업이라 단위 테스트가 아니라 **옛 이름이 하나도 안 남았는지**와 **빌드·기존 테스트 통과**로 검증한다.

**Files:**
- Modify: `src/shared/styles/_tokens.scss` (전면 재작성)
- Modify: `src/app/styles/global.scss`
- Modify: `src/features/auth/ui/AuthLayout.module.scss`
- Modify: `src/features/auth/ui/AuthCard.module.scss`
- Modify: `src/pages/HomePage/HomePage.module.scss`
- Modify: `src/shared/ui/TextField/TextField.module.scss`
- Modify: `src/shared/ui/Checkbox/Checkbox.module.scss`

**Interfaces:**
- Consumes: 없음
- Produces: `@mixin dark-tokens` (`src/shared/styles/_tokens.scss`) — Task 5 의 `AuthLayout` 다크 고정이 쓴다. 토큰 이름 `--bg-sunken` `--bg` `--surface` `--text-strong` `--text` `--text-muted` `--text-subtle` `--accent` `--accent-ink` `--danger` `--shadow-card` `--wafer-glow`

- [ ] **Step 1: 현재 옛 이름 사용처를 기록해둔다**

Run:
```bash
yarn oxlint --version && git grep -n -E -- '--(bg-deep|bg-base|bg-glow|text-hi|text-dim|text-faint)' src | wc -l
```
Expected: 0 이 아닌 수(약 20). 이 수가 Step 5 에서 0 이 되어야 한다.

- [ ] **Step 2: `_tokens.scss` 를 재작성한다**

`src/shared/styles/_tokens.scss` 전체를 아래로 교체:

```scss
// 런타임에 바뀌는 값과 테마 값은 CSS 커스텀 프로퍼티로 둔다.
// 컴파일 타임 상수(브레이크포인트 등)는 _mixins.scss 의 SCSS 변수로.
//
// 이름은 톤이 아니라 역할로 짓는다. '--bg-deep'(깊은 어둠) 같은 이름은
// 라이트 모드에서 뜻이 없어서 테마를 두 벌로 만드는 순간 걸림돌이 된다.

@mixin dark-tokens {
  --bg-sunken: #070c13;
  --bg: #0a1119;

  // 반투명을 유지한다. AuthCard 가 이걸 통해 뒤쪽 웨이퍼 글로우를 비친다.
  // 불투명하게 바꾸면 로그인 카드가 납작해진다.
  --surface: rgba(12, 22, 34, 0.74);
  --surface-input: rgba(255, 255, 255, 0.035);

  --border: rgba(120, 190, 220, 0.16);
  --border-input: rgba(140, 190, 215, 0.14);
  --border-faint: rgba(140, 190, 215, 0.13);

  --text-strong: #eaf3f8;
  --text: #8aa1b4;
  --text-muted: #6e869b;
  --text-subtle: #4e6376;
  --text-placeholder: #5c7085;

  // 버튼 위에는 반드시 --accent-ink 를 얹는다 (대비 10.4:1)
  --accent: #22d3ee;
  --accent-hi: #67e8f9;
  --accent-lo: #0ea5e9;
  --accent-ink: #04121b;
  --accent-soft: rgba(34, 211, 238, 0.055);
  --accent-ring: rgba(34, 211, 238, 0.12);

  --danger: #fb7185;
  --danger-soft: rgba(251, 113, 133, 0.1);

  --shadow-card:
    0 26px 64px rgba(0, 0, 0, 0.58), inset 0 1px 0 rgba(160, 220, 240, 0.06);

  color-scheme: dark;
}

:root {
  // 라이트가 기본값이다.
  --bg-sunken: #eef1f5;
  --bg: #f4f6f9;

  --surface: #ffffff;
  --surface-input: #f8fafc;

  --border: #e2e8f0;
  --border-input: #d7dee7;
  --border-faint: #e8edf3;

  --text-strong: #0f1c2b;
  --text: #334155;
  --text-muted: #64748b;
  // 대비 2.9:1 — 장식성 라벨 전용이다. 본문에 쓰지 말 것.
  --text-subtle: #94a3b8;
  --text-placeholder: #94a3b8;

  // 시안(#22d3ee)은 흰 배경에서 대비 1.6:1 이라 쓸 수 없다.
  // 라이트에서는 관계가 뒤집힌다 — 어두운 청록 바탕에 흰 글자(6.4:1).
  --accent: #0e7490;
  --accent-hi: #0891b2;
  --accent-lo: #155e75;
  --accent-ink: #ffffff;
  --accent-soft: rgba(14, 116, 144, 0.06);
  --accent-ring: rgba(14, 116, 144, 0.16);

  --danger: #b91c1c;
  --danger-soft: rgba(185, 28, 28, 0.07);

  --shadow-card: 0 1px 2px rgba(15, 28, 43, 0.06);

  color-scheme: light;

  // 웨이퍼 배경 전용. 어두운 배경이 전제라 테마를 따르지 않는다.
  // 로그인 화면은 항상 다크이므로 짝이 필요 없다.
  --wafer-glow: #12253a;

  // 형태 — 장비 UI 답게 각지게
  --radius-xs: 3px;
  --radius-sm: 4px;
  --radius-md: 6px;

  --font-sans: 'Wanted Sans Variable', system-ui, 'Segoe UI', sans-serif;
  --font-mono: ui-monospace, Consolas, 'D2Coding', monospace;
}

// 아무것도 고르지 않았으면 OS 설정을 따른다.
@media (prefers-color-scheme: dark) {
  :root:not([data-theme='light']) {
    @include dark-tokens;
  }
}

// 사용자가 명시적으로 고르면 OS 설정을 이긴다.
:root[data-theme='dark'] {
  @include dark-tokens;
}
```

- [ ] **Step 3: `global.scss` 의 body 를 고친다**

`src/app/styles/global.scss` 에서 두 줄을 바꾼다:

```scss
body {
  margin: 0;
  font-family: var(--font-sans);
  font-weight: 400;
  font-size: 15px;
  line-height: 1.5;
  color: var(--text);
  background: var(--bg-sunken);   // 였던 것: var(--bg-deep)
  text-rendering: optimizeLegibility;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}
```

**`color-scheme: dark;` 줄은 지운다.** `_tokens.scss` 의 `:root` 와 `dark-tokens` 가 각각 선언하므로 여기 남으면 라이트 모드에서 스크롤바가 어둡게 나온다.

- [ ] **Step 4: 나머지 5개 스타일 파일의 토큰 이름을 치환한다**

`AuthLayout.module.scss` · `AuthCard.module.scss` · `HomePage.module.scss` · `TextField.module.scss` · `Checkbox.module.scss` 에서:

| 찾기 | 바꾸기 |
|---|---|
| `--bg-deep` | `--bg-sunken` |
| `--bg-base` | `--bg` |
| `--bg-glow` | `--wafer-glow` |
| `--text-hi` | `--text-strong` |
| `--text-dim` | `--text-muted` |
| `--text-faint` | `--text-subtle` |

`--surface` · `--border*` · `--accent*` · `--danger*` · `--shadow-card` · `--radius-*` 는 이름이 그대로다.

- [ ] **Step 5: 옛 이름이 남지 않았는지 확인한다**

Run:
```bash
git grep -n -E -- '--(bg-deep|bg-base|bg-glow|text-hi|text-dim|text-faint)' src
```
Expected: 출력 없음 (exit 1). 하나라도 나오면 그 파일을 마저 고친다.

- [ ] **Step 6: 빌드와 기존 테스트가 통과하는지 확인한다**

Run:
```bash
yarn lint && yarn build && yarn test
```
Expected: 전부 통과. 테스트 45개 pass.

- [ ] **Step 7: 커밋**

```bash
git add src/shared/styles/_tokens.scss src/app/styles/global.scss src/features/auth/ui/AuthLayout.module.scss src/features/auth/ui/AuthCard.module.scss src/pages/HomePage/HomePage.module.scss src/shared/ui/TextField/TextField.module.scss src/shared/ui/Checkbox/Checkbox.module.scss
git commit -m "refactor: 토큰 이름을 역할 기반으로 바꾸고 라이트 팔레트 추가

테마를 두 벌로 만들려면 '--bg-deep'(깊은 어둠) 같은 톤 기반 이름이
라이트 모드에서 뜻을 잃는다. 값은 아직 다크만 쓰이므로 화면은 그대로다."
```

---

### Task 2: themeStore — 테마 상태를 Redux 밖에 둔다

**Files:**
- Create: `src/shared/theme/themeStore.ts`
- Create: `src/shared/theme/themeStore.test.ts`
- Create: `src/shared/theme/index.ts`

**Interfaces:**
- Consumes: 없음
- Produces:
  - `type Theme = 'light' | 'dark'`
  - `type ThemePreference = Theme | 'system'`
  - `themeStore.get(): ThemePreference`
  - `themeStore.getResolved(): Theme`
  - `themeStore.set(next: ThemePreference): void`
  - `themeStore.subscribe(listener: () => void): () => void`
  - `THEME_STORAGE_KEY = 'ibs.theme'`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`src/shared/theme/themeStore.test.ts`:

```ts
import { afterEach, beforeEach, describe, expect, it, vi } from 'vitest'
import { THEME_STORAGE_KEY, themeStore } from './themeStore'

const setSystemDark = (matches: boolean) => {
  vi.stubGlobal(
    'matchMedia',
    vi.fn().mockReturnValue({
      matches,
      addEventListener: vi.fn(),
      removeEventListener: vi.fn(),
    }),
  )
}

describe('themeStore', () => {
  beforeEach(() => {
    localStorage.clear()
    delete document.documentElement.dataset.theme
    setSystemDark(false)
    themeStore.set('system')
  })

  afterEach(() => {
    vi.unstubAllGlobals()
  })

  it('아무것도 고르지 않으면 system 이고 data-theme 속성이 없다', () => {
    expect(themeStore.get()).toBe('system')
    expect(document.documentElement.dataset.theme).toBeUndefined()
    expect(localStorage.getItem(THEME_STORAGE_KEY)).toBeNull()
  })

  it('dark 를 고르면 속성과 localStorage 를 함께 바꾼다', () => {
    themeStore.set('dark')

    expect(themeStore.get()).toBe('dark')
    expect(document.documentElement.dataset.theme).toBe('dark')
    expect(localStorage.getItem(THEME_STORAGE_KEY)).toBe('dark')
  })

  // 속성만 지우고 localStorage 를 안 지우면 다음 방문에 되살아난다
  it('system 으로 되돌리면 속성과 저장값이 모두 사라진다', () => {
    themeStore.set('dark')
    themeStore.set('system')

    expect(document.documentElement.dataset.theme).toBeUndefined()
    expect(localStorage.getItem(THEME_STORAGE_KEY)).toBeNull()
  })

  it('system 일 때 getResolved 는 OS 설정을 따른다', () => {
    setSystemDark(true)
    themeStore.set('system')
    expect(themeStore.getResolved()).toBe('dark')

    setSystemDark(false)
    themeStore.set('system')
    expect(themeStore.getResolved()).toBe('light')
  })

  it('명시적으로 고르면 getResolved 가 OS 설정을 무시한다', () => {
    setSystemDark(true)
    themeStore.set('light')
    expect(themeStore.getResolved()).toBe('light')
  })

  it('구독자에게 변경을 알리고 해제할 수 있다', () => {
    const listener = vi.fn()
    const unsubscribe = themeStore.subscribe(listener)

    themeStore.set('dark')
    expect(listener).toHaveBeenCalledTimes(1)

    unsubscribe()
    themeStore.set('light')
    expect(listener).toHaveBeenCalledTimes(1)
  })
})
```

- [ ] **Step 2: 실패를 확인한다**

Run: `yarn vitest run src/shared/theme/themeStore.test.ts`
Expected: FAIL — `Failed to resolve import "./themeStore"`

- [ ] **Step 3: 구현한다**

`src/shared/theme/themeStore.ts`:

```ts
export type Theme = 'light' | 'dark'
export type ThemePreference = Theme | 'system'

export const THEME_STORAGE_KEY = 'ibs.theme'

const DARK_QUERY = '(prefers-color-scheme: dark)'

const listeners = new Set<() => void>()

// 시크릿 모드나 저장소 차단 환경에서 localStorage 접근은 throw 한다.
// 테마 때문에 앱이 죽으면 안 되므로 전부 감싼다.
const readStored = (): ThemePreference => {
  try {
    const value = localStorage.getItem(THEME_STORAGE_KEY)
    return value === 'light' || value === 'dark' ? value : 'system'
  } catch {
    return 'system'
  }
}

const writeStored = (next: ThemePreference): void => {
  try {
    if (next === 'system') localStorage.removeItem(THEME_STORAGE_KEY)
    else localStorage.setItem(THEME_STORAGE_KEY, next)
  } catch {
    // 저장에 실패해도 이번 세션에서는 동작한다
  }
}

const applyToDom = (next: ThemePreference): void => {
  const root = document.documentElement
  if (next === 'system') delete root.dataset.theme
  else root.dataset.theme = next
}

const systemPrefersDark = (): boolean => {
  try {
    return window.matchMedia(DARK_QUERY).matches
  } catch {
    return false
  }
}

let preference: ThemePreference = readStored()

export const themeStore = {
  get: (): ThemePreference => preference,

  getResolved: (): Theme =>
    preference === 'system' ? (systemPrefersDark() ? 'dark' : 'light') : preference,

  set: (next: ThemePreference): void => {
    preference = next
    writeStored(next)
    applyToDom(next)
    listeners.forEach((listener) => listener())
  },

  subscribe: (listener: () => void): (() => void) => {
    listeners.add(listener)
    return () => {
      listeners.delete(listener)
    }
  },
}

// system 을 고른 상태에서 OS 테마가 바뀌면 CSS 는 미디어 쿼리로 알아서 따라가지만
// 토글 아이콘은 그대로 남는다. 그 아이콘을 갱신하려고 구독한다.
try {
  window.matchMedia(DARK_QUERY).addEventListener('change', () => {
    if (preference === 'system') listeners.forEach((listener) => listener())
  })
} catch {
  // matchMedia 가 없는 환경에서는 건너뛴다
}
```

`src/shared/theme/index.ts`:

```ts
export { themeStore, THEME_STORAGE_KEY } from './themeStore'
export type { Theme, ThemePreference } from './themeStore'
```

- [ ] **Step 4: 통과를 확인한다**

Run: `yarn vitest run src/shared/theme/themeStore.test.ts`
Expected: PASS — 6 tests

- [ ] **Step 5: 첫 페인트 전 적용 스크립트를 넣는다**

`index.html` 의 `<head>` 안, `<title>` 아래에 추가:

```html
    <title>company-app</title>
    <!--
      React 이펙트는 첫 페인트 뒤에 돈다. 여기서 미리 박지 않으면
      새로고침할 때마다 반대 테마가 한 번 번쩍인다.
    -->
    <script>
      try {
        var t = localStorage.getItem('ibs.theme')
        if (t === 'light' || t === 'dark') document.documentElement.dataset.theme = t
      } catch (e) {}
    </script>
```

- [ ] **Step 6: 전체 검증**

Run: `yarn lint && yarn tsc -b && yarn test`
Expected: 전부 통과

- [ ] **Step 7: 커밋**

```bash
git add src/shared/theme index.html
git commit -m "feat: 테마 저장소 추가

진실을 <html data-theme> 속성에 두고 Redux 에 넣지 않는다. store 에 복사하면
index.html 의 첫 페인트 스크립트와 진실이 두 곳으로 갈린다."
```

---

### Task 3: useTheme 훅과 ThemeToggle 버튼

**Files:**
- Create: `src/shared/theme/useTheme.ts`
- Create: `src/shared/ui/ThemeToggle/ThemeToggle.tsx`
- Create: `src/shared/ui/ThemeToggle/ThemeToggle.module.scss`
- Create: `src/shared/ui/ThemeToggle/index.ts`
- Create: `src/shared/ui/ThemeToggle/ThemeToggle.test.tsx`
- Modify: `src/shared/theme/index.ts`

**Interfaces:**
- Consumes: `themeStore`, `Theme`, `ThemePreference` (Task 2)
- Produces:
  - `useTheme(): { preference: ThemePreference; resolved: Theme; setPreference: (next: ThemePreference) => void }`
  - `<ThemeToggle />` — props 없음

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`src/shared/ui/ThemeToggle/ThemeToggle.test.tsx`:

```tsx
import { render, screen } from '@testing-library/react'
import userEvent from '@testing-library/user-event'
import { afterEach, beforeEach, describe, expect, it, vi } from 'vitest'
import { themeStore } from '@/shared/theme'
import { ThemeToggle } from './ThemeToggle'

describe('ThemeToggle', () => {
  beforeEach(() => {
    vi.stubGlobal(
      'matchMedia',
      vi.fn().mockReturnValue({
        matches: false,
        addEventListener: vi.fn(),
        removeEventListener: vi.fn(),
      }),
    )
    localStorage.clear()
    themeStore.set('system')
  })

  afterEach(() => {
    vi.unstubAllGlobals()
  })

  // 아이콘만으로는 무엇인지 알 수 없다
  it('현재 테마가 아니라 바뀔 테마를 라벨로 알린다', () => {
    render(<ThemeToggle />)
    expect(screen.getByRole('button', { name: '다크 모드로 전환' })).toBeInTheDocument()
  })

  it('누르면 반대 테마로 명시 전환한다', async () => {
    const user = userEvent.setup()
    render(<ThemeToggle />)

    await user.click(screen.getByRole('button'))

    expect(themeStore.get()).toBe('dark')
    expect(document.documentElement.dataset.theme).toBe('dark')
    expect(screen.getByRole('button', { name: '라이트 모드로 전환' })).toBeInTheDocument()
  })
})
```

- [ ] **Step 2: 실패를 확인한다**

Run: `yarn vitest run src/shared/ui/ThemeToggle/ThemeToggle.test.tsx`
Expected: FAIL — `Failed to resolve import "./ThemeToggle"`

- [ ] **Step 3: 훅을 구현한다**

`src/shared/theme/useTheme.ts`:

```ts
import { useSyncExternalStore } from 'react'
import { themeStore } from './themeStore'
import type { Theme, ThemePreference } from './themeStore'

export interface UseThemeResult {
  preference: ThemePreference
  resolved: Theme
  setPreference: (next: ThemePreference) => void
}

// 테마는 리액트 밖(DOM 속성 + localStorage)에 산다.
// useSyncExternalStore 가 정확히 그런 상태를 구독하기 위한 훅이다.
export const useTheme = (): UseThemeResult => {
  const preference = useSyncExternalStore(themeStore.subscribe, themeStore.get, themeStore.get)
  const resolved = useSyncExternalStore(
    themeStore.subscribe,
    themeStore.getResolved,
    themeStore.getResolved,
  )

  return { preference, resolved, setPreference: themeStore.set }
}
```

`src/shared/theme/index.ts` 에 추가:

```ts
export { themeStore, THEME_STORAGE_KEY } from './themeStore'
export type { Theme, ThemePreference } from './themeStore'
export { useTheme } from './useTheme'
export type { UseThemeResult } from './useTheme'
```

- [ ] **Step 4: 버튼을 구현한다**

`src/shared/ui/ThemeToggle/ThemeToggle.tsx`:

```tsx
import { useTheme } from '@/shared/theme'
import styles from './ThemeToggle.module.scss'

// 3상태(light·dark·system)를 한 버튼으로 순환시키면 무엇이 선택됐는지 알기 어렵다.
// system 은 아무것도 고르지 않은 초기값으로만 두고, 누르면 명시적으로 정한다.
export const ThemeToggle = () => {
  const { resolved, setPreference } = useTheme()
  const next = resolved === 'dark' ? 'light' : 'dark'

  return (
    <button
      type="button"
      className={styles.toggle}
      aria-label={next === 'dark' ? '다크 모드로 전환' : '라이트 모드로 전환'}
      onClick={() => setPreference(next)}
    >
      <span aria-hidden="true">{resolved === 'dark' ? '☾' : '☀'}</span>
    </button>
  )
}
```

`src/shared/ui/ThemeToggle/ThemeToggle.module.scss`:

```scss
@use '@/shared/styles' as s;

.toggle {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  width: 32px;
  height: 32px;
  padding: 0;
  border: 1px solid transparent;
  border-radius: var(--radius-sm);
  background: transparent;
  color: var(--text-muted);
  font-size: 15px;
  line-height: 1;
  cursor: pointer;
  transition: color 0.15s ease, background 0.15s ease;

  &:hover {
    color: var(--text-strong);
    background: var(--accent-soft);
  }

  @include s.focus-ring;

  @include s.reduced-motion {
    transition: none;
  }
}
```

`src/shared/ui/ThemeToggle/index.ts`:

```ts
export { ThemeToggle } from './ThemeToggle'
```

- [ ] **Step 5: 통과를 확인한다**

Run: `yarn vitest run src/shared/ui/ThemeToggle/ThemeToggle.test.tsx`
Expected: PASS — 2 tests

- [ ] **Step 6: 커밋**

```bash
git add src/shared/theme src/shared/ui/ThemeToggle
git commit -m "feat: 테마 토글 버튼 추가

3상태 순환 대신 2상태로 둔다. system 은 아무것도 고르지 않은 초기값일 뿐이고,
누르는 순간 명시적으로 정해진다 - 순환시키면 무엇이 선택됐는지 알기 어렵다."
```

---

### Task 4: `me` 쿼리

**Files:**
- Modify: `src/features/auth/api/types.ts`
- Modify: `src/features/auth/api/authApi.ts`
- Modify: `src/features/auth/index.ts`
- Modify: `src/shared/api/baseApi.ts`
- Create: `src/features/auth/api/authApi.me.test.tsx`

**Interfaces:**
- Consumes: `baseApi` (`src/shared/api`), `createTestWrapper` · `jsonResponse` (`src/test/storeWrapper`)
- Produces:
  - `type Role = 'USER' | 'ADMIN'`
  - `type UserStatus = 'PENDING' | 'APPROVED' | 'REJECTED'`
  - `interface Me { id: number; email: string; name: string; role: Role; status: UserStatus }`
  - `useMeQuery()` — `features/auth` 의 `index.ts` 로 export

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`src/features/auth/api/authApi.me.test.tsx`:

```tsx
import { renderHook, waitFor } from '@testing-library/react'
import { afterEach, describe, expect, it, vi } from 'vitest'
import { createTestWrapper, jsonResponse } from '@/test/storeWrapper'
import { useMeQuery } from './authApi'

const ME = {
  id: 1,
  email: 'tiger@ibslab.com',
  name: '김성호',
  role: 'ADMIN',
  status: 'APPROVED',
}

describe('useMeQuery', () => {
  afterEach(() => {
    vi.unstubAllGlobals()
  })

  it('내 정보를 가져온다', async () => {
    vi.stubGlobal('fetch', vi.fn().mockResolvedValue(jsonResponse(ME)))
    const { wrapper } = createTestWrapper()

    const { result } = renderHook(() => useMeQuery(), { wrapper })

    await waitFor(() => expect(result.current.data).toEqual(ME))
  })

  // 헤더 조각들이 각자 부르므로 이게 성립하지 않으면 요청이 조각 수만큼 나간다
  it('여러 곳에서 불러도 요청은 한 번이다', async () => {
    const fetchMock = vi.fn().mockResolvedValue(jsonResponse(ME))
    vi.stubGlobal('fetch', fetchMock)
    const { wrapper } = createTestWrapper()

    const { result } = renderHook(
      () => {
        useMeQuery()
        useMeQuery()
        return useMeQuery()
      },
      { wrapper },
    )

    await waitFor(() => expect(result.current.data).toEqual(ME))
    expect(fetchMock).toHaveBeenCalledTimes(1)
  })

  // 승인 대기·거절 상태도 200 으로 온다. 403 이면 이유를 화면에 띄울 수 없다.
  it('REJECTED 상태도 그대로 받는다', async () => {
    vi.stubGlobal(
      'fetch',
      vi.fn().mockResolvedValue(jsonResponse({ ...ME, role: 'USER', status: 'REJECTED' })),
    )
    const { wrapper } = createTestWrapper()

    const { result } = renderHook(() => useMeQuery(), { wrapper })

    await waitFor(() => expect(result.current.data?.status).toBe('REJECTED'))
  })
})
```

- [ ] **Step 2: 실패를 확인한다**

Run: `yarn vitest run src/features/auth/api/authApi.me.test.tsx`
Expected: FAIL — `useMeQuery` 를 export 하지 않음

- [ ] **Step 3: 타입을 추가한다**

`src/features/auth/api/types.ts` 끝에 추가:

```ts
export type Role = 'USER' | 'ADMIN'
export type UserStatus = 'PENDING' | 'APPROVED' | 'REJECTED'

/** GET /api/users/me */
export interface Me {
  id: number
  email: string
  name: string
  role: Role
  status: UserStatus
}
```

- [ ] **Step 4: baseApi 에 태그를 선언한다**

`src/shared/api/baseApi.ts` 를 아래로 교체:

```ts
import { createApi } from '@reduxjs/toolkit/query/react'
import { baseQuery } from './baseQuery'

// 빈 껍데기로 두고 각 feature 가 injectEndpoints 로 주입한다.
// 여기에 엔드포인트를 직접 적으면 shared 가 도메인을 알게 되어 의존 방향이 뒤집힌다.
//
// 태그 이름만 예외다. RTK Query 는 createApi 시점에 tagTypes 를 알아야 하고,
// 이름은 문자열일 뿐이라 도메인 코드가 딸려 오지 않는다.
export const baseApi = createApi({
  reducerPath: 'api',
  baseQuery,
  tagTypes: ['Me', 'AdminUsers'],
  endpoints: () => ({}),
})
```

- [ ] **Step 5: `me` 엔드포인트를 추가한다**

`src/features/auth/api/authApi.ts` 의 `import` 에 `Me` 를 더하고:

```ts
import type { LoginRequest, LoginResponse, Me } from './types'
```

`endpoints` 안에 `login` 앞에 추가:

```ts
    me: build.query<Me, void>({
      query: () => '/users/me',
      providesTags: ['Me'],
    }),
```

파일 끝의 export 를 교체:

```ts
export const { useMeQuery, useLoginMutation, useSignupMutation, useLogoutMutation } = authApi
```

- [ ] **Step 6: feature 밖으로 내보낸다**

`src/features/auth/index.ts` 에 추가:

```ts
export { useMeQuery } from './api/authApi'
export type { Me, Role, UserStatus } from './api/types'
```

- [ ] **Step 7: 통과를 확인한다**

Run: `yarn vitest run src/features/auth/api/authApi.me.test.tsx`
Expected: PASS — 3 tests

- [ ] **Step 8: 전체 검증과 커밋**

Run: `yarn lint && yarn tsc -b && yarn test`

```bash
git add src/features/auth src/shared/api/baseApi.ts
git commit -m "feat: GET /api/users/me 연결

헤더 조각들이 각자 useMeQuery 를 부른다. 캐시 키가 같아 요청은 한 번이고,
레이아웃이 me 를 받아 내려주는 props 사슬이 생기지 않는다."
```

---

### Task 5: AppLayout 과 AppHeader — widgets 레이어 신설

**Files:**
- Create: `src/widgets/AppHeader/AppHeader.tsx`
- Create: `src/widgets/AppHeader/UserMenu.tsx`
- Create: `src/widgets/AppHeader/AppHeader.module.scss`
- Create: `src/widgets/AppHeader/index.ts`
- Create: `src/app/layouts/AppLayout.tsx`
- Create: `src/app/layouts/AppLayout.module.scss`
- Create: `src/widgets/AppHeader/AppHeader.test.tsx`
- Modify: `src/app/routes.tsx`
- Modify: `src/pages/HomePage/HomePage.tsx`
- Modify: `src/features/auth/ui/AuthLayout.module.scss`
- Modify: `src/test/storeWrapper.tsx`

**Interfaces:**
- Consumes: `useMeQuery` · `useLogout` (`@/features/auth`), `ThemeToggle` (`@/shared/ui/ThemeToggle`)
- Produces:
  - `<AppHeader />` — props 없음
  - `<AppLayout />` — props 없음. 라우트 `element` 로 쓴다
  - `createTestWrapper({ withRouter?: boolean })` — authSlice 리듀서 포함

- [ ] **Step 1: 테스트 래퍼에 authSlice 를 넣는다**

`src/test/storeWrapper.tsx` 를 아래로 교체:

```tsx
import { configureStore } from '@reduxjs/toolkit'
import { Provider } from 'react-redux'
import { MemoryRouter } from 'react-router'
import type { ReactNode } from 'react'
import { authSlice } from '@/features/auth'
import { baseApi } from '@/shared/api'

export interface TestWrapperOptions {
  /** MemoryRouter 로 감싼다. <Link> 를 쓰는 컴포넌트에 필요하다 */
  withRouter?: boolean
  /** MemoryRouter 초기 경로 */
  route?: string
}

// RTK Query 는 결과를 캐싱한다. 테스트마다 새 store 를 만들지 않으면
// 앞 테스트의 응답이 다음 테스트로 새어 들어간다.
export const createTestWrapper = ({ withRouter = false, route = '/' }: TestWrapperOptions = {}) => {
  const store = configureStore({
    reducer: {
      [baseApi.reducerPath]: baseApi.reducer,
      [authSlice.reducerPath]: authSlice.reducer,
    },
    middleware: (getDefault) => getDefault().concat(baseApi.middleware),
  })

  const wrapper = ({ children }: { children: ReactNode }) => (
    <Provider store={store}>
      {withRouter ? <MemoryRouter initialEntries={[route]}>{children}</MemoryRouter> : children}
    </Provider>
  )

  return { store, wrapper }
}

export const jsonResponse = (body: unknown, status = 200) =>
  new Response(JSON.stringify(body), {
    status,
    headers: { 'Content-Type': 'application/json' },
  })

/** 201 처럼 본문 없이 성공하는 응답 */
export const emptyResponse = (status = 201) => new Response(null, { status })
```

- [ ] **Step 2: 실패하는 테스트를 쓴다**

`src/widgets/AppHeader/AppHeader.test.tsx`:

```tsx
import { render, screen, waitFor } from '@testing-library/react'
import { afterEach, beforeEach, describe, expect, it, vi } from 'vitest'
import { createTestWrapper, jsonResponse } from '@/test/storeWrapper'
import { AppHeader } from './AppHeader'

const ME = {
  id: 1,
  email: 'tiger@ibslab.com',
  name: '김성호',
  role: 'USER',
  status: 'APPROVED',
}

describe('AppHeader', () => {
  beforeEach(() => {
    vi.stubGlobal(
      'matchMedia',
      vi.fn().mockReturnValue({
        matches: false,
        addEventListener: vi.fn(),
        removeEventListener: vi.fn(),
      }),
    )
  })

  afterEach(() => {
    vi.unstubAllGlobals()
  })

  it('me 가 오면 이름을 보여준다', async () => {
    vi.stubGlobal('fetch', vi.fn().mockResolvedValue(jsonResponse(ME)))
    const { wrapper } = createTestWrapper({ withRouter: true })

    render(<AppHeader />, { wrapper })

    await waitFor(() => expect(screen.getByText('김성호')).toBeInTheDocument())
  })

  // 이름이 툭 나타나면 옆 요소들이 밀린다
  it('me 가 오기 전에도 이름 자리를 차지한다', () => {
    vi.stubGlobal('fetch', vi.fn(() => new Promise(() => {})))
    const { wrapper } = createTestWrapper({ withRouter: true })

    render(<AppHeader />, { wrapper })

    expect(screen.getByTestId('user-name')).toBeInTheDocument()
    expect(screen.getByTestId('user-name')).toHaveTextContent('')
  })

  it('로고가 홈으로 간다', () => {
    vi.stubGlobal('fetch', vi.fn().mockResolvedValue(jsonResponse(ME)))
    const { wrapper } = createTestWrapper({ withRouter: true })

    render(<AppHeader />, { wrapper })

    expect(screen.getByRole('link', { name: 'IBS 홈' })).toHaveAttribute('href', '/')
  })

  it('로그아웃 버튼이 있다', () => {
    vi.stubGlobal('fetch', vi.fn().mockResolvedValue(jsonResponse(ME)))
    const { wrapper } = createTestWrapper({ withRouter: true })

    render(<AppHeader />, { wrapper })

    expect(screen.getByRole('button', { name: '로그아웃' })).toBeInTheDocument()
  })
})
```

- [ ] **Step 3: 실패를 확인한다**

Run: `yarn vitest run src/widgets/AppHeader/AppHeader.test.tsx`
Expected: FAIL — `Failed to resolve import "./AppHeader"`

- [ ] **Step 4: UserMenu 를 구현한다**

`src/widgets/AppHeader/UserMenu.tsx`:

```tsx
import { useMeQuery, useLogout } from '@/features/auth'
import styles from './AppHeader.module.scss'

// 드롭다운으로 만들지 않는다. 항목이 이름과 로그아웃 둘뿐인데
// aria-expanded·Escape·포커스 트랩을 직접 챙길 이유가 없다.
export const UserMenu = () => {
  const { data: me } = useMeQuery()
  const { logout, isLoading } = useLogout()

  return (
    <div className={styles.user}>
      {/* me 가 오기 전에도 자리를 차지해야 옆 요소가 밀리지 않는다 */}
      <span className={styles.userName} data-testid="user-name">
        {me?.name ?? ''}
      </span>
      <button type="button" className={styles.logout} disabled={isLoading} aria-busy={isLoading} onClick={logout}>
        로그아웃
      </button>
    </div>
  )
}
```

- [ ] **Step 5: AppHeader 를 구현한다**

`src/widgets/AppHeader/AppHeader.tsx`:

```tsx
import { Link } from 'react-router'
import { ThemeToggle } from '@/shared/ui/ThemeToggle'
import { UserMenu } from './UserMenu'
import styles from './AppHeader.module.scss'

// 내비게이션 자리는 .spacer 여백뿐이다. 넣을 업무 메뉴가 아직 없어서
// MainNav 컴포넌트를 만들면 null 을 반환하는 빈 껍데기가 된다.
export const AppHeader = () => {
  return (
    <header className={styles.header}>
      <Link to="/" className={styles.logo} aria-label="IBS 홈">
        IBS
      </Link>

      <div className={styles.spacer} />

      <ThemeToggle />
      <UserMenu />
    </header>
  )
}
```

`src/widgets/AppHeader/index.ts`:

```ts
export { AppHeader } from './AppHeader'
```

- [ ] **Step 6: 스타일을 쓴다**

`src/widgets/AppHeader/AppHeader.module.scss`:

```scss
@use '@/shared/styles' as s;

.header {
  display: flex;
  align-items: center;
  gap: 14px;
  height: 52px;
  padding: 0 20px;
  border-bottom: 1px solid var(--border);
  background: var(--surface);
}

.logo {
  color: var(--text-strong);
  font-weight: 600;
  font-size: 14px;
  letter-spacing: 0.14em;
  text-decoration: none;

  @include s.focus-ring;
}

.spacer {
  flex: 1;
}

.user {
  display: flex;
  align-items: center;
  gap: 12px;
}

.userName {
  min-width: 3.5em;
  color: var(--text);
  font-size: 13px;
  text-align: right;

  @include s.below(s.$bp-mobile) {
    display: none;
  }
}

.logout {
  padding: 6px 10px;
  border: 1px solid var(--border);
  border-radius: var(--radius-sm);
  background: transparent;
  color: var(--text-muted);
  font-size: 13px;
  cursor: pointer;
  transition: color 0.15s ease, border-color 0.15s ease;

  &:hover:not(:disabled) {
    color: var(--text-strong);
    border-color: var(--accent-ring);
  }

  &:disabled {
    cursor: not-allowed;
    opacity: 0.6;
  }

  @include s.focus-ring;

  @include s.reduced-motion {
    transition: none;
  }
}
```

- [ ] **Step 7: AppLayout 을 만든다**

`src/app/layouts/AppLayout.tsx`:

```tsx
import { Outlet } from 'react-router'
import { AppHeader } from '@/widgets/AppHeader'
import styles from './AppLayout.module.scss'

// 헤더를 여기서 소유한다. 각 페이지가 따로 렌더하면 라우트 이동마다
// 언마운트·재마운트되어 포커스와 열린 UI 상태가 날아간다.
// AuthLayout 이 WaferCanvas 를 소유하는 것과 같은 이유다.
export const AppLayout = () => {
  return (
    <div className={styles.shell}>
      <AppHeader />
      <main className={styles.content}>
        <Outlet />
      </main>
    </div>
  )
}
```

`src/app/layouts/AppLayout.module.scss`:

```scss
.shell {
  display: flex;
  flex-direction: column;
  min-height: 100%;
  background: var(--bg);
}

.content {
  flex: 1;
  padding: 28px 20px;
}
```

- [ ] **Step 8: 라우트를 갈아끼운다**

`src/app/routes.tsx` 를 아래로 교체:

```tsx
import { createBrowserRouter, Navigate } from 'react-router'
import { AuthLayout, RedirectIfAuthenticated, RequireAuth } from '@/features/auth'
import { AppLayout } from './layouts/AppLayout'
import { HomePage } from '@/pages/HomePage'
import { LoginPage } from '@/pages/LoginPage'
import { SignupPage } from '@/pages/SignupPage'

export const router = createBrowserRouter([
  {
    // 가드가 바깥, 레이아웃이 안쪽이다. 반대로 두면 인증되지 않은 사용자에게
    // 헤더가 한 번 그려졌다 사라진다.
    element: <RequireAuth />,
    children: [
      {
        element: <AppLayout />,
        children: [{ index: true, element: <HomePage /> }],
      },
    ],
  },
  {
    // AuthLayout 이 WaferCanvas 를 소유한다. /login ↔ /signup 을 오가도
    // 캔버스가 재마운트되지 않는다.
    element: <RedirectIfAuthenticated />,
    children: [
      {
        element: <AuthLayout />,
        children: [
          { path: 'login', element: <LoginPage /> },
          { path: 'signup', element: <SignupPage /> },
        ],
      },
    ],
  },
  { path: '*', element: <Navigate to="/" replace /> },
])
```

- [ ] **Step 9: HomePage 에서 로그아웃 버튼을 뺀다**

로그아웃은 이제 헤더에 있다. `src/pages/HomePage/HomePage.tsx` 를 아래로 교체:

```tsx
import styles from './HomePage.module.scss'

export const HomePage = () => {
  return (
    <>
      <title>홈 · IBS</title>
      <h1 className={styles.title}>환영합니다</h1>
    </>
  )
}
```

`src/pages/HomePage/HomePage.module.scss` 에서 `.page` 와 `.actions` 규칙을 지운다. 레이아웃은 `AppLayout` 의 `.content` 가 맡는다. `.title` 만 남긴다.

- [ ] **Step 10: AuthLayout 을 다크로 고정한다**

최상위 클래스는 `.page` 다. `src/features/auth/ui/AuthLayout.module.scss` 의 1행과 3~21행을 아래로 교체한다 (Task 1 Step 4 의 토큰 이름 치환이 이미 반영된 상태여야 한다):

```scss
@use '@/shared/styles' as s;
@use '@/shared/styles/tokens' as t;

.page {
  // 웨이퍼 노광은 어두운 배경에 다이가 빛나는 것이 개념의 전부다.
  // 커스텀 프로퍼티는 상속되므로 이 한 줄로 이 서브트리만 다크가 된다.
  // 아래 background 의 var() 도 여기서 선언한 값을 읽는다.
  @include t.dark-tokens;

  display: flex;
  align-items: center;
  justify-content: center;
  gap: clamp(24px, 4vw, 60px);
  min-height: 100svh;
  padding:
    32px
    max(24px, env(safe-area-inset-right))
    32px
    max(24px, env(safe-area-inset-left));
  box-sizing: border-box;
  background: radial-gradient(
    900px 620px at 32% 45%,
    var(--wafer-glow) 0%,
    var(--bg) 60%,
    var(--bg-sunken) 100%
  );
}
```

`@use ... as t` 로 별칭을 붙여야 `_mixins.scss` 를 forward 하는 `s` 와 충돌하지 않는다. 22행 이하(`.waferPane` 등)는 그대로 둔다.

- [ ] **Step 11: 통과를 확인한다**

Run: `yarn vitest run src/widgets/AppHeader/AppHeader.test.tsx`
Expected: PASS — 4 tests

- [ ] **Step 12: 화면으로 확인한다**

`.claude/launch.json` 의 dev 서버를 띄우고 `/login` 으로 로그인한 뒤:
- 헤더에 `IBS` · 테마 아이콘 · 이름 · 로그아웃이 보인다
- 테마 아이콘을 눌러 라이트/다크가 바뀌고 **새로고침해도 유지되며 번쩍이지 않는다**
- `/login` 은 테마와 무관하게 항상 다크다
- 로그아웃이 동작한다

- [ ] **Step 13: 전체 검증과 커밋**

Run: `yarn lint && yarn tsc -b && yarn test && yarn build`

```bash
git add src/widgets src/app/layouts src/app/routes.tsx src/pages/HomePage src/features/auth/ui/AuthLayout.module.scss src/test/storeWrapper.tsx
git commit -m "feat: 앱 셸 추가 - AppLayout 과 AppHeader

헤더를 레이아웃 라우트가 소유해 라우트 이동에 재마운트되지 않게 한다.
헤더 조각은 props 를 받지 않고 각자 useMeQuery 를 구독한다.

widgets 레이어를 신설했다. 헤더는 여러 feature 를 모으는 조립물이라
features 중 어느 하나에 넣으면 그 feature 가 나머지를 알아야 한다."
```

---

### Task 6: 관리자 링크와 RequireAdmin 가드

**Files:**
- Create: `src/widgets/AppHeader/AdminLink.tsx`
- Create: `src/features/auth/ui/RequireAdmin.tsx`
- Create: `src/features/auth/ui/RequireAdmin.test.tsx`
- Create: `src/widgets/AppHeader/AdminLink.test.tsx`
- Modify: `src/widgets/AppHeader/AppHeader.tsx`
- Modify: `src/widgets/AppHeader/AppHeader.module.scss`
- Modify: `src/features/auth/index.ts`

**Interfaces:**
- Consumes: `useMeQuery` (Task 4)
- Produces: `<AdminLink />`, `<RequireAdmin />` (라우트 `element` 로 쓴다)

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`src/widgets/AppHeader/AdminLink.test.tsx`:

```tsx
import { render, screen, waitFor } from '@testing-library/react'
import { afterEach, describe, expect, it, vi } from 'vitest'
import { createTestWrapper, jsonResponse } from '@/test/storeWrapper'
import { AdminLink } from './AdminLink'

const meWith = (role: string) => ({
  id: 1,
  email: 'tiger@ibslab.com',
  name: '김성호',
  role,
  status: 'APPROVED',
})

describe('AdminLink', () => {
  afterEach(() => {
    vi.unstubAllGlobals()
  })

  it('관리자에게는 승인 관리 링크를 보여준다', async () => {
    vi.stubGlobal('fetch', vi.fn().mockResolvedValue(jsonResponse(meWith('ADMIN'))))
    const { wrapper } = createTestWrapper({ withRouter: true })

    render(<AdminLink />, { wrapper })

    await waitFor(() =>
      expect(screen.getByRole('link', { name: '승인 관리' })).toHaveAttribute(
        'href',
        '/admin/users',
      ),
    )
  })

  it('일반 사용자에게는 아무것도 보여주지 않는다', async () => {
    vi.stubGlobal('fetch', vi.fn().mockResolvedValue(jsonResponse(meWith('USER'))))
    const { wrapper } = createTestWrapper({ withRouter: true })

    render(<AdminLink />, { wrapper })

    await waitFor(() => expect(screen.queryByRole('link')).not.toBeInTheDocument())
  })
})
```

`src/features/auth/ui/RequireAdmin.test.tsx`:

```tsx
import { render, screen, waitFor } from '@testing-library/react'
import { Route, Routes } from 'react-router'
import { afterEach, describe, expect, it, vi } from 'vitest'
import { createTestWrapper, jsonResponse } from '@/test/storeWrapper'
import { RequireAdmin } from './RequireAdmin'

const meWith = (role: string) => ({
  id: 1,
  email: 'tiger@ibslab.com',
  name: '김성호',
  role,
  status: 'APPROVED',
})

const renderAt = (route: string) => {
  const { wrapper } = createTestWrapper({ withRouter: true, route })
  return render(
    <Routes>
      <Route path="/" element={<p>홈</p>} />
      <Route element={<RequireAdmin />}>
        <Route path="/admin/users" element={<p>승인 관리</p>} />
      </Route>
    </Routes>,
    { wrapper },
  )
}

describe('RequireAdmin', () => {
  afterEach(() => {
    vi.unstubAllGlobals()
  })

  // role 을 모르는 상태에서 판단하면 관리자인데도 홈으로 튕긴다.
  // RequireAuth 가 status === 'unknown' 일 때 아무것도 안 그리는 것과 같은 이유다.
  it('me 로딩 중에는 아무것도 렌더하지 않는다', () => {
    vi.stubGlobal('fetch', vi.fn(() => new Promise(() => {})))
    renderAt('/admin/users')

    expect(screen.queryByText('승인 관리')).not.toBeInTheDocument()
    expect(screen.queryByText('홈')).not.toBeInTheDocument()
  })

  it('관리자는 통과시킨다', async () => {
    vi.stubGlobal('fetch', vi.fn().mockResolvedValue(jsonResponse(meWith('ADMIN'))))
    renderAt('/admin/users')

    await waitFor(() => expect(screen.getByText('승인 관리')).toBeInTheDocument())
  })

  it('일반 사용자는 홈으로 보낸다', async () => {
    vi.stubGlobal('fetch', vi.fn().mockResolvedValue(jsonResponse(meWith('USER'))))
    renderAt('/admin/users')

    await waitFor(() => expect(screen.getByText('홈')).toBeInTheDocument())
  })
})
```

- [ ] **Step 2: 실패를 확인한다**

Run: `yarn vitest run src/widgets/AppHeader/AdminLink.test.tsx src/features/auth/ui/RequireAdmin.test.tsx`
Expected: FAIL — 두 모듈 모두 없음

- [ ] **Step 3: AdminLink 를 구현한다**

`src/widgets/AppHeader/AdminLink.tsx`:

```tsx
import { NavLink } from 'react-router'
import { useMeQuery } from '@/features/auth'
import styles from './AppHeader.module.scss'

// 여기서 숨기는 것은 화면 정리일 뿐 보안이 아니다.
// 실제 차단은 백엔드 AdminUserService.verifyAdmin 이 매 요청 DB 로 한다.
export const AdminLink = () => {
  const { data: me } = useMeQuery()

  if (me?.role !== 'ADMIN') return null

  // <nav> 를 여기 두는 이유 — 헤더에 두면 일반 사용자에게 빈 랜드마크가 남는다.
  // NavLink 가 현재 경로일 때 aria-current="page" 를 알아서 붙인다.
  return (
    <nav aria-label="관리">
      <NavLink
        to="/admin/users"
        className={({ isActive }) =>
          isActive ? `${styles.navLink} ${styles.navLinkActive}` : styles.navLink
        }
      >
        승인 관리
      </NavLink>
    </nav>
  )
}
```

- [ ] **Step 4: RequireAdmin 을 구현한다**

`src/features/auth/ui/RequireAdmin.tsx`:

```tsx
import { Navigate, Outlet } from 'react-router'
import { useMeQuery } from '../api/authApi'

// me 가 도착하기 전에는 판단하지 않는다. role 을 모르는 채로 리다이렉트하면
// 관리자인데도 홈으로 튕긴다. RequireAuth 의 'unknown' 처리와 같은 규칙이다.
export const RequireAdmin = () => {
  const { data: me, isLoading } = useMeQuery()

  if (isLoading || !me) return null
  if (me.role !== 'ADMIN') return <Navigate to="/" replace />
  return <Outlet />
}
```

`src/features/auth/index.ts` 에 추가:

```ts
export { RequireAdmin } from './ui/RequireAdmin'
```

- [ ] **Step 5: 헤더에 끼우고 스타일을 더한다**

`src/widgets/AppHeader/AppHeader.tsx` 에서 `<div className={styles.spacer} />` 다음, `<ThemeToggle />` 앞에 `<AdminLink />` 를 넣고 import 를 추가한다:

```tsx
import { AdminLink } from './AdminLink'
```

`src/widgets/AppHeader/AppHeader.module.scss` 끝에 추가:

```scss
.navLink {
  padding: 6px 10px;
  border-radius: var(--radius-sm);
  color: var(--text-muted);
  font-size: 13px;
  text-decoration: none;

  &:hover {
    color: var(--text-strong);
    background: var(--accent-soft);
  }

  @include s.focus-ring;
}

.navLinkActive {
  color: var(--accent);
  background: var(--accent-soft);
}
```

- [ ] **Step 6: 통과를 확인한다**

Run: `yarn vitest run src/widgets/AppHeader src/features/auth/ui/RequireAdmin.test.tsx`
Expected: PASS — 6 tests (AppHeader 4 + AdminLink 2 는 별도 파일, RequireAdmin 3)

- [ ] **Step 7: 전체 검증과 커밋**

Run: `yarn lint && yarn tsc -b && yarn test`

```bash
git add src/widgets/AppHeader src/features/auth
git commit -m "feat: 관리자 링크와 RequireAdmin 가드

me 가 도착하기 전에는 아무것도 렌더하지 않는다. role 을 모르는 채로 판단하면
관리자인데도 홈으로 튕긴다.

프론트의 role 은 신뢰 경계가 아니라 화면 정리용이다."
```

---

### Task 7: features/admin — 승인 관리 API

**Files:**
- Create: `src/features/admin/api/types.ts`
- Create: `src/features/admin/api/adminUsersApi.ts`
- Create: `src/features/admin/api/adminUsersApi.test.tsx`
- Create: `src/features/admin/index.ts`

**Interfaces:**
- Consumes: `baseApi` (`@/shared/api`), `UserStatus` (`@/features/auth`)
- Produces:
  - `interface AdminUser { id: number; email: string; name: string; status: UserStatus; createdAt: string }`
  - `useAdminUsersQuery(status: UserStatus)`
  - `useApproveUserMutation()` — 인자는 `id: number`
  - `useRejectUserMutation()` — 인자는 `id: number`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`src/features/admin/api/adminUsersApi.test.tsx`:

```tsx
import { act, renderHook, waitFor } from '@testing-library/react'
import { afterEach, describe, expect, it, vi } from 'vitest'
import { createTestWrapper, emptyResponse, jsonResponse } from '@/test/storeWrapper'
import { useAdminUsersQuery, useApproveUserMutation } from './adminUsersApi'

const PENDING = [
  {
    id: 2,
    email: 'younghee@ibslab.com',
    name: '이영희',
    status: 'PENDING',
    createdAt: '2026-08-17T14:22:00',
  },
]

describe('adminUsersApi', () => {
  afterEach(() => {
    vi.unstubAllGlobals()
  })

  it('상태를 쿼리 파라미터로 넘겨 목록을 가져온다', async () => {
    const fetchMock = vi.fn().mockResolvedValue(jsonResponse(PENDING))
    vi.stubGlobal('fetch', fetchMock)
    const { wrapper } = createTestWrapper()

    const { result } = renderHook(() => useAdminUsersQuery('PENDING'), { wrapper })

    await waitFor(() => expect(result.current.data).toEqual(PENDING))
    // fetchBaseQuery 는 Request 객체를 넘기지만, 문자열로 넘어와도 깨지지 않게 둔다
    const [sent] = fetchMock.mock.calls[0]
    expect(String(sent?.url ?? sent)).toContain('/admin/users?status=PENDING')
  })

  // 승인하면 목록에서 사라져야 한다. 태그를 안 걸면 화면이 그대로 남는다.
  it('승인하면 목록을 다시 가져온다', async () => {
    const fetchMock = vi
      .fn()
      .mockResolvedValueOnce(jsonResponse(PENDING))
      .mockResolvedValueOnce(emptyResponse(204))
      .mockResolvedValueOnce(jsonResponse([]))
    vi.stubGlobal('fetch', fetchMock)
    const { wrapper } = createTestWrapper()

    const { result } = renderHook(
      () => ({
        list: useAdminUsersQuery('PENDING'),
        approve: useApproveUserMutation(),
      }),
      { wrapper },
    )

    await waitFor(() => expect(result.current.list.data).toHaveLength(1))

    await act(async () => {
      await result.current.approve[0](2).unwrap()
    })

    await waitFor(() => expect(result.current.list.data).toHaveLength(0))
  })
})
```

- [ ] **Step 2: 실패를 확인한다**

Run: `yarn vitest run src/features/admin/api/adminUsersApi.test.tsx`
Expected: FAIL — `Failed to resolve import "./adminUsersApi"`

- [ ] **Step 3: 타입을 쓴다**

`src/features/admin/api/types.ts`:

```ts
import type { UserStatus } from '@/features/auth'

/** GET /api/admin/users?status= 의 항목 */
export interface AdminUser {
  id: number
  email: string
  name: string
  status: UserStatus
  /** KST 기준 ISO 문자열. 백엔드가 타임존 없이 내려준다 */
  createdAt: string
}
```

- [ ] **Step 4: 엔드포인트를 구현한다**

`src/features/admin/api/adminUsersApi.ts`:

```ts
import { baseApi } from '@/shared/api'
import type { UserStatus } from '@/features/auth'
import type { AdminUser } from './types'

// 낙관적 갱신을 쓰지 않는다. 목록이 짧고 사람이 하나씩 누르는 화면이라
// 롤백 코드를 두는 것보다 재조회 한 번이 싸다.
export const adminUsersApi = baseApi.injectEndpoints({
  endpoints: (build) => ({
    adminUsers: build.query<AdminUser[], UserStatus>({
      query: (status) => `/admin/users?status=${status}`,
      providesTags: ['AdminUsers'],
    }),

    approveUser: build.mutation<void, number>({
      query: (id) => ({ url: `/admin/users/${id}/approve`, method: 'POST' }),
      invalidatesTags: ['AdminUsers'],
    }),

    rejectUser: build.mutation<void, number>({
      query: (id) => ({ url: `/admin/users/${id}/reject`, method: 'POST' }),
      invalidatesTags: ['AdminUsers'],
    }),
  }),
})

export const { useAdminUsersQuery, useApproveUserMutation, useRejectUserMutation } = adminUsersApi
```

`src/features/admin/index.ts`:

```ts
export {
  useAdminUsersQuery,
  useApproveUserMutation,
  useRejectUserMutation,
} from './api/adminUsersApi'
export type { AdminUser } from './api/types'
```

- [ ] **Step 5: 통과를 확인한다**

Run: `yarn vitest run src/features/admin/api/adminUsersApi.test.tsx`
Expected: PASS — 2 tests

- [ ] **Step 6: 커밋**

```bash
git add src/features/admin
git commit -m "feat: 승인 관리 API 연결

admin 을 auth 와 별도 feature 로 둔다. auth 는 '내가 로그인한다'에 머물고,
'관리자가 남의 상태를 바꾼다'는 다른 도메인이다."
```

---

### Task 8: 승인 관리 화면

**Files:**
- Create: `src/features/admin/ui/UserApprovalList.tsx`
- Create: `src/features/admin/ui/UserApprovalList.module.scss`
- Create: `src/features/admin/ui/UserApprovalList.test.tsx`
- Create: `src/pages/AdminUsersPage/AdminUsersPage.tsx`
- Create: `src/pages/AdminUsersPage/AdminUsersPage.module.scss`
- Create: `src/pages/AdminUsersPage/index.ts`
- Modify: `src/features/admin/index.ts`
- Modify: `src/app/routes.tsx`

**Interfaces:**
- Consumes: `useAdminUsersQuery` · `useApproveUserMutation` · `useRejectUserMutation` (Task 7), `useMeQuery` (Task 4), `RequireAdmin` (Task 6)
- Produces: `<UserApprovalList />` (props 없음), `<AdminUsersPage />`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`src/features/admin/ui/UserApprovalList.test.tsx`:

```tsx
import { render, screen, waitFor } from '@testing-library/react'
import { afterEach, describe, expect, it, vi } from 'vitest'
import { createTestWrapper, jsonResponse } from '@/test/storeWrapper'
import { UserApprovalList } from './UserApprovalList'

const ME = { id: 1, email: 'tiger@ibslab.com', name: '김성호', role: 'ADMIN', status: 'APPROVED' }

const row = (id: number, name: string) => ({
  id,
  name,
  email: `user${id}@ibslab.com`,
  status: 'PENDING',
  createdAt: '2026-08-17T14:22:00',
})

// me 와 목록 두 요청이 섞여 나가므로 URL 로 갈라준다
const stubFetch = (list: unknown) =>
  vi.stubGlobal(
    'fetch',
    vi.fn((input: Request | string) => {
      const url = typeof input === 'string' ? input : input.url
      return Promise.resolve(url.includes('/users/me') ? jsonResponse(ME) : jsonResponse(list))
    }),
  )

describe('UserApprovalList', () => {
  afterEach(() => {
    vi.unstubAllGlobals()
  })

  it('승인 대기 목록을 보여준다', async () => {
    stubFetch([row(2, '이영희')])
    const { wrapper } = createTestWrapper()

    render(<UserApprovalList />, { wrapper })

    await waitFor(() => expect(screen.getByText('이영희')).toBeInTheDocument())
    expect(screen.getByText('user2@ibslab.com')).toBeInTheDocument()
  })

  // 백엔드가 CANNOT_REJECT_SELF 로 400 을 준다. 눌러서 에러를 보는 것보다 낫다.
  it('자기 자신의 거절 버튼은 비활성이다', async () => {
    stubFetch([row(1, '김성호'), row(2, '이영희')])
    const { wrapper } = createTestWrapper()

    render(<UserApprovalList />, { wrapper })

    await waitFor(() => expect(screen.getByText('이영희')).toBeInTheDocument())

    const rejectButtons = screen.getAllByRole('button', { name: '거절' })
    expect(rejectButtons[0]).toBeDisabled()
    expect(rejectButtons[1]).toBeEnabled()
  })

  it('비어 있으면 빈 표 대신 안내를 보여준다', async () => {
    stubFetch([])
    const { wrapper } = createTestWrapper()

    render(<UserApprovalList />, { wrapper })

    await waitFor(() =>
      expect(screen.getByText('승인 대기 중인 사람이 없습니다')).toBeInTheDocument(),
    )
  })

  it('탭 세 개가 있다', async () => {
    stubFetch([])
    const { wrapper } = createTestWrapper()

    render(<UserApprovalList />, { wrapper })

    expect(screen.getByRole('tab', { name: /승인 대기/ })).toBeInTheDocument()
    expect(screen.getByRole('tab', { name: '승인됨' })).toBeInTheDocument()
    // 실수로 거절했을 때 되돌릴 유일한 통로다
    expect(screen.getByRole('tab', { name: '거절됨' })).toBeInTheDocument()
  })
})
```

- [ ] **Step 2: 실패를 확인한다**

Run: `yarn vitest run src/features/admin/ui/UserApprovalList.test.tsx`
Expected: FAIL — `Failed to resolve import "./UserApprovalList"`

- [ ] **Step 3: 목록 컴포넌트를 구현한다**

`src/features/admin/ui/UserApprovalList.tsx`:

```tsx
import { useState } from 'react'
import { useMeQuery } from '@/features/auth'
import type { UserStatus } from '@/features/auth'
import {
  useAdminUsersQuery,
  useApproveUserMutation,
  useRejectUserMutation,
} from '../api/adminUsersApi'
import styles from './UserApprovalList.module.scss'

const TABS: { status: UserStatus; label: string; empty: string }[] = [
  { status: 'PENDING', label: '승인 대기', empty: '승인 대기 중인 사람이 없습니다' },
  { status: 'APPROVED', label: '승인됨', empty: '승인된 사람이 없습니다' },
  // 실수로 거절했을 때 되돌릴 유일한 통로다. 거절해도 users 행이 남아
  // 당사자는 같은 주소로 재가입할 수 없다.
  { status: 'REJECTED', label: '거절됨', empty: '거절된 사람이 없습니다' },
]

const formatJoinedAt = (value: string): string =>
  value.replace('T', ' ').slice(5, 16)

export const UserApprovalList = () => {
  const [status, setStatus] = useState<UserStatus>('PENDING')
  const { data: me } = useMeQuery()
  const { data: users, isFetching } = useAdminUsersQuery(status)
  const [approve, approveState] = useApproveUserMutation()
  const [reject, rejectState] = useRejectUserMutation()

  const busy = approveState.isLoading || rejectState.isLoading
  const tab = TABS.find((t) => t.status === status) ?? TABS[0]

  return (
    <div>
      <div className={styles.tabs} role="tablist">
        {TABS.map((t) => (
          <button
            key={t.status}
            type="button"
            role="tab"
            aria-selected={t.status === status}
            className={t.status === status ? `${styles.tab} ${styles.tabActive}` : styles.tab}
            onClick={() => setStatus(t.status)}
          >
            {t.label}
            {t.status === 'PENDING' && users && status === 'PENDING' && users.length > 0 && (
              <span className={styles.count}>{users.length}</span>
            )}
          </button>
        ))}
      </div>

      <div className={styles.panel} role="tabpanel" aria-busy={isFetching}>
        {users?.length === 0 && <p className={styles.empty}>{tab.empty}</p>}

        {users?.map((user) => (
          <div key={user.id} className={styles.row}>
            <div className={styles.info}>
              <div className={styles.name}>{user.name}</div>
              <div className={styles.meta}>
                {user.email} · {formatJoinedAt(user.createdAt)}
              </div>
            </div>

            {status !== 'APPROVED' && (
              <button
                type="button"
                className={styles.approve}
                disabled={busy}
                aria-busy={busy}
                onClick={() => approve(user.id)}
              >
                승인
              </button>
            )}

            {status !== 'REJECTED' && (
              <button
                type="button"
                className={styles.reject}
                // 백엔드가 CANNOT_REJECT_SELF 로 400 을 준다. 미리 막는다.
                disabled={busy || me?.id === user.id}
                aria-busy={busy}
                onClick={() => reject(user.id)}
              >
                거절
              </button>
            )}
          </div>
        ))}
      </div>

      {/* 행이 사라지는 것만으로는 스크린리더에 전달되지 않는다 */}
      <p role="status" className="visually-hidden">
        {approveState.isSuccess && '승인했습니다.'}
        {rejectState.isSuccess && '거절했습니다.'}
      </p>
    </div>
  )
}
```

- [ ] **Step 4: 스타일을 쓴다**

`src/features/admin/ui/UserApprovalList.module.scss`:

```scss
@use '@/shared/styles' as s;

.tabs {
  display: flex;
  gap: 2px;
  border-bottom: 1px solid var(--border);
}

.tab {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  margin-bottom: -1px;
  padding: 9px 15px;
  border: 0;
  border-bottom: 2px solid transparent;
  background: transparent;
  color: var(--text-muted);
  font-size: 13px;
  cursor: pointer;

  @include s.focus-ring;
}

.tabActive {
  border-bottom-color: var(--accent);
  color: var(--accent);
  font-weight: 600;
}

.count {
  padding: 1px 6px;
  border-radius: 9px;
  background: var(--accent);
  color: var(--accent-ink);
  font-size: 11px;
}

.panel {
  border: 1px solid var(--border);
  border-top: 0;
  border-radius: 0 0 var(--radius-md) var(--radius-md);
  background: var(--surface);
}

.empty {
  margin: 0;
  padding: 34px 16px;
  color: var(--text-muted);
  font-size: 13px;
  text-align: center;
}

.row {
  display: flex;
  align-items: center;
  gap: 10px;
  padding: 12px 14px;

  & + & {
    border-top: 1px solid var(--border-faint);
  }

  @include s.below(s.$bp-mobile) {
    flex-wrap: wrap;
  }
}

.info {
  flex: 1;
  min-width: 0;

  @include s.below(s.$bp-mobile) {
    flex-basis: 100%;
  }
}

.name {
  color: var(--text-strong);
  font-size: 13px;
  font-weight: 600;
}

.meta {
  margin-top: 2px;
  color: var(--text-muted);
  font-size: 11.5px;
  overflow-wrap: anywhere;
}

.approve,
.reject {
  padding: 6px 14px;
  border-radius: var(--radius-sm);
  font-size: 12px;
  cursor: pointer;

  &:disabled {
    cursor: not-allowed;
    opacity: 0.45;
  }

  @include s.focus-ring;

  @include s.below(s.$bp-mobile) {
    flex: 1;
  }
}

.approve {
  border: 0;
  background: var(--accent);
  color: var(--accent-ink);
  font-weight: 600;
}

.reject {
  border: 1px solid var(--danger);
  background: transparent;
  color: var(--danger);
}
```

`src/features/admin/index.ts` 에 추가:

```ts
export { UserApprovalList } from './ui/UserApprovalList'
```

- [ ] **Step 5: 페이지와 라우트를 만든다**

`src/pages/AdminUsersPage/AdminUsersPage.tsx`:

```tsx
import { UserApprovalList } from '@/features/admin'
import styles from './AdminUsersPage.module.scss'

export const AdminUsersPage = () => {
  return (
    <>
      <title>승인 관리 · IBS</title>
      <h1 className={styles.title}>승인 관리</h1>
      <UserApprovalList />
    </>
  )
}
```

`src/pages/AdminUsersPage/AdminUsersPage.module.scss`:

```scss
.title {
  margin: 0 0 16px;
  color: var(--text-strong);
  font-size: 20px;
}
```

`src/pages/AdminUsersPage/index.ts`:

```ts
export { AdminUsersPage } from './AdminUsersPage'
```

`src/app/routes.tsx` 의 `AppLayout` children 을 아래로 교체하고 import 두 개를 더한다:

```tsx
import { AuthLayout, RedirectIfAuthenticated, RequireAdmin, RequireAuth } from '@/features/auth'
import { AdminUsersPage } from '@/pages/AdminUsersPage'
```

```tsx
        children: [
          { index: true, element: <HomePage /> },
          {
            element: <RequireAdmin />,
            children: [{ path: 'admin/users', element: <AdminUsersPage /> }],
          },
        ],
```

- [ ] **Step 6: 통과를 확인한다**

Run: `yarn vitest run src/features/admin`
Expected: PASS — 6 tests (api 2 + ui 4)

- [ ] **Step 7: 화면으로 확인한다**

관리자 계정으로 로그인해 `/admin/users` 로 간다. DB 에 관리자가 없으면 백엔드에서 한 번만:

```sql
UPDATE users SET status='APPROVED', role='ADMIN' WHERE email='tiger@ibslab.com';
```

- 탭 세 개가 보이고 전환된다
- 승인을 누르면 목록에서 사라진다
- 자기 자신 행의 거절 버튼이 비활성이다
- 라이트·다크 양쪽에서 읽힌다
- 창을 좁히면 버튼이 아래로 내려간다

- [ ] **Step 8: 전체 검증과 커밋**

Run: `yarn lint && yarn tsc -b && yarn test && yarn build`

```bash
git add src/features/admin src/pages/AdminUsersPage src/app/routes.tsx
git commit -m "feat: 승인 관리 화면 추가

status 가 필수 파라미터라 탭 하나가 요청 하나에 그대로 대응한다.

거절됨 탭을 반드시 둔다. 거절해도 users 행이 남아 당사자는 재가입할 수 없어서,
실수로 거절했을 때 되돌릴 방법이 이 탭에서 다시 승인하는 것뿐이다."
```

---

### Task 9: 승인 상태에 따른 사용자 경험

관리자 쪽을 만들었으니 당사자 쪽도 맞춘다.

**Files:**
- Modify: `src/features/auth/model/useLogin.ts`
- Modify: `src/features/auth/model/useLogin.test.tsx`
- Modify: `src/features/auth/ui/SignupForm.tsx` (가입 성공 후 안내 문구)
- Modify: `src/features/auth/ui/LoginForm.tsx` (배너 문구)
- Create: `src/features/auth/model/useRejectedGuard.ts`
- Modify: `src/app/layouts/AppLayout.tsx`

**Interfaces:**
- Consumes: `useMeQuery` · `useLogout` (`@/features/auth`), `toErrorInfo` (`@/shared/api`)
- Produces: `useRejectedGuard(): void`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`src/features/auth/model/useLogin.test.tsx` 에 추가:

```tsx
  // 403 은 자격 증명 문제가 아니라 승인 상태 문제다. 401 과 섞으면
  // "이메일 또는 비밀번호가 올바르지 않습니다" 가 떠서 사용자가 계속 다시 시도한다.
  it('승인 대기 중이면 그 사실을 알린다', async () => {
    vi.stubGlobal(
      'fetch',
      vi.fn().mockResolvedValue(
        jsonResponse({ code: 'APPROVAL_PENDING', message: '관리자 승인 대기 중입니다.' }, 403),
      ),
    )
    const { wrapper } = createTestWrapper()
    const { result } = renderHook(() => useLogin(), { wrapper })

    await act(async () => {
      await result.current.submit(CREDENTIALS)
    })

    expect(result.current.formError).toBe('관리자 승인 대기 중입니다. 승인 후 이용할 수 있습니다.')
  })

  it('거절된 계정이면 그 사실을 알린다', async () => {
    vi.stubGlobal(
      'fetch',
      vi.fn().mockResolvedValue(
        jsonResponse({ code: 'SIGNUP_REJECTED', message: '가입이 거절되었습니다.' }, 403),
      ),
    )
    const { wrapper } = createTestWrapper()
    const { result } = renderHook(() => useLogin(), { wrapper })

    await act(async () => {
      await result.current.submit(CREDENTIALS)
    })

    expect(result.current.formError).toBe('가입이 거절되었습니다. 관리자에게 문의해 주세요.')
  })
```

- [ ] **Step 2: 실패를 확인한다**

Run: `yarn vitest run src/features/auth/model/useLogin.test.tsx`
Expected: FAIL — 서버가 준 원문이 그대로 나와 기대 문구와 다르다

- [ ] **Step 3: useLogin 을 고친다**

`src/features/auth/model/useLogin.ts` 의 `catch` 블록을 아래로 교체:

```ts
      } catch (e) {
        const info = toErrorInfo(e)
        setFormError(messageFor(info))
        return false
      }
```

그리고 파일 상단(`useLogin` 정의 앞)에 추가:

```ts
// 403 은 자격 증명이 아니라 승인 상태의 문제다. 401 문구로 덮으면
// 사용자가 비밀번호를 계속 다시 친다.
const APPROVAL_MESSAGES: Record<string, string> = {
  APPROVAL_PENDING: '관리자 승인 대기 중입니다. 승인 후 이용할 수 있습니다.',
  SIGNUP_REJECTED: '가입이 거절되었습니다. 관리자에게 문의해 주세요.',
}

const messageFor = (info: ApiErrorInfo): string => {
  if (info.code && APPROVAL_MESSAGES[info.code]) return APPROVAL_MESSAGES[info.code]
  // 401 은 어느 쪽이 틀렸는지 알려주지 않는다. 계정 열거를 막기 위해서다.
  if (info.kind === 'UNAUTHORIZED') return '이메일 또는 비밀번호가 올바르지 않습니다.'
  return info.message
}
```

import 에 타입을 더한다:

```ts
import { toErrorInfo } from '@/shared/api'
import type { ApiErrorInfo } from '@/shared/api'
```

- [ ] **Step 4: 통과를 확인한다**

Run: `yarn vitest run src/features/auth/model/useLogin.test.tsx`
Expected: PASS — 7 tests

- [ ] **Step 5: 가입 성공 안내를 고친다**

`src/features/auth/ui/LoginForm.tsx:98` 의 배너 문구가 **승인제 도입으로 거짓말이 됐다.** 승인 전에는 비밀번호가 맞아도 로그인되지 않는다.

```tsx
        {signedUpEmail && !formError && (
          <p className={styles.formNotice} role="status">
            가입이 완료됐습니다. 비밀번호를 입력해 로그인하세요.
          </p>
        )}
```

이 문구만 아래로 교체한다 (조건과 `role="status"` 는 그대로):

```tsx
            가입 신청이 접수됐습니다. 관리자 승인 후 로그인할 수 있습니다.
```

**`SignupForm.tsx` 는 고치지 않는다.** 187행의 `가입 완료` 는 제출 버튼의 상태 표시일 뿐 로그인 가능 여부를 주장하지 않는다.

- [ ] **Step 6: 세션 중 거절을 처리한다**

`src/features/auth/model/useRejectedGuard.ts`:

```ts
import { useEffect } from 'react'
import { useMeQuery } from '../api/authApi'
import { useLogout } from './useLogout'

// 관리자가 거절해도 access token 은 서명만으로 검증되어 서버가 취소할 수 없다.
// 그래서 최대 30분간 그대로 쓰인다. me 의 status 를 보고 이 브라우저에서만 끊는다.
// 완전한 차단이 아니라 완화책이다 - 직접 요청을 만드는 클라이언트에는 통하지 않는다.
export const useRejectedGuard = (): void => {
  const { data: me } = useMeQuery()
  const { logout } = useLogout()

  useEffect(() => {
    if (me?.status === 'REJECTED') void logout()
  }, [me?.status, logout])
}
```

`src/features/auth/index.ts` 에 추가:

```ts
export { useRejectedGuard } from './model/useRejectedGuard'
```

`src/app/layouts/AppLayout.tsx` 에서 호출한다:

```tsx
import { Outlet } from 'react-router'
import { useRejectedGuard } from '@/features/auth'
import { AppHeader } from '@/widgets/AppHeader'
import styles from './AppLayout.module.scss'

export const AppLayout = () => {
  useRejectedGuard()

  return (
    <div className={styles.shell}>
      <AppHeader />
      <main className={styles.content}>
        <Outlet />
      </main>
    </div>
  )
}
```

- [ ] **Step 7: 전체 검증과 커밋**

Run: `yarn lint && yarn tsc -b && yarn test && yarn build`

```bash
git add src/features/auth src/app/layouts/AppLayout.tsx
git commit -m "feat: 승인 상태를 사용자에게 알린다

가입 후 '로그인해 주세요' 안내는 거짓말이었다 - 승인 전에는 로그인이 막힌다.

403 을 401 문구로 덮으면 사용자가 비밀번호를 계속 다시 친다.
세션 중 거절은 me 의 status 로 감지해 이 브라우저에서만 끊는다."
```

---

### Task 10: 문서 갱신

**Files:**
- Modify: `CLAUDE.md`
- Create: `docs/api/users.md`

**Interfaces:**
- Consumes: 없음
- Produces: 없음

- [ ] **Step 1: `docs/api/users.md` 를 계약 문서로 쓴다**

`docs/api/auth.md` 와 같은 형식으로, 구현된 사실을 적는다:

```markdown
# 사용자 API 명세

갱신일: 2026-08-17
상태: **구현 완료.**

프론트(`front/company-app`)와 백엔드(`backend/company-backend`)가 함께 참조한다.

## 1. `GET /api/users/me`

| | |
|---|---|
| 인증 | 필요 (`Authorization: Bearer`) |
| 성공 | 200 |

```json
{ "id": 1, "email": "tiger@ibslab.com", "name": "김성호", "role": "ADMIN", "status": "APPROVED" }
```

`role` 은 `USER | ADMIN`, `status` 는 `PENDING | APPROVED | REJECTED`.

**승인 게이트가 걸려 있지 않다.** `PENDING` · `REJECTED` 여도 403 이 아니라 200 에 그 상태가 담겨 온다. 여기서 403 을 내면 프론트가 "승인 대기 중" 화면을 그릴 근거를 잃는다.

**역할은 토큰이 아니라 이 엔드포인트로 안다.** 클레임에 담으면 강등해도 토큰 수명 동안 관리자로 남는다.

## 2. 관리자 API

| 엔드포인트 | 성공 | 비고 |
|---|---|---|
| `GET /api/admin/users?status=` | 200 `[{id, email, name, status, createdAt}]` | `status` 는 **필수** |
| `POST /api/admin/users/{id}/approve` | 204 | |
| `POST /api/admin/users/{id}/reject` | 204 | 해당 사용자 refresh 토큰을 전부 폐기한다 |

| HTTP | `code` | 상황 |
|---|---|---|
| 403 | `FORBIDDEN` | 관리자가 아님 |
| 404 | `USER_NOT_FOUND` | |
| 400 | `CANNOT_REJECT_SELF` | 프론트가 버튼을 미리 비활성화한다 |

**거절해도 `users` 행은 남는다.** 그 사람은 같은 주소로 재가입할 수 없다(이메일 유니크). 되돌리려면 거절됨 목록에서 다시 승인해야 한다.

## 3. 첫 관리자

승인 API 를 쓰려면 관리자가 있어야 하는데 관리자를 만들 API 도 관리자를 요구해서 순환이다. 새 환경에서는 최초 1회만 SQL 로 끊는다.

```sql
UPDATE users SET status='APPROVED', role='ADMIN' WHERE email='...';
```

## 4. 알려진 한계

`reject` 는 refresh 토큰을 폐기하지만 **access token 은 무효화할 수 없다.** 거절 후 최대 30분(`access-token-ttl`)간 인증 API 가 열려 있다.

프론트는 `me` 의 `status` 가 `REJECTED` 면 즉시 로그아웃시킨다(`useRejectedGuard`). **브라우저에서만 통하는 완화책이다.** 근본 해법은 백엔드 판단으로 남겨뒀다.
```

- [ ] **Step 2: `CLAUDE.md` 를 갱신한다**

세 곳을 고친다.

**구조 절** — 레이어 다이어그램에 `widgets` 를 넣는다:

```
src/
├── app/         조립 계층 + 라우팅 + store. 여기만 아래를 다 안다
├── pages/       라우트 단위 화면 (Home · Login · Signup · AdminUsers)
├── widgets/     여러 feature 를 모으는 조립물 (AppHeader)
├── features/    도메인 단위 (auth · admin)
└── shared/      도메인을 모르는 것들
```

의존 방향 문장을 `app → pages → widgets → features → shared` 로 고치고, `entities`, `widgets` 는 아직 없다는 문장에서 `widgets` 를 뺀다. 그리고 한 줄 더한다:

> **`widgets` 는 여러 feature 를 조립하는 UI 를 둔다.** `AppHeader` 가 auth 의 `me` 와 앞으로의 업무 메뉴를 한자리에 모으는데, `features/auth` 에 두면 auth 가 나머지 도메인을 알아야 한다.

**라우팅 절** — 표를 갱신한다:

```
/             RequireAuth → AppLayout → HomePage
/admin/users  RequireAuth → AppLayout → RequireAdmin → AdminUsersPage
/login        RedirectIfAuthenticated → AuthLayout → LoginPage
/signup       RedirectIfAuthenticated → AuthLayout → SignupPage
```

> **`AppLayout` 이 `AppHeader` 를 소유한다.** `AuthLayout` 이 `WaferCanvas` 를 소유하는 것과 같은 이유다 — 페이지마다 헤더를 렌더하면 라우트 이동마다 재마운트된다.
>
> **`RequireAdmin` 은 `me` 로딩 중에 아무것도 렌더하지 않는다.** `role` 을 모르는 채 판단하면 관리자인데도 홈으로 튕긴다.

**스타일 절** — 테마 항목을 새로 넣는다:

> ### 테마 — 다크 · 라이트
>
> `_tokens.scss` 가 라이트를 `:root` 기본값으로 두고, `@mixin dark-tokens` 를 미디어 쿼리와 `[data-theme='dark']` 두 곳에 건다. 상태는 셋이다 — 명시적 라이트, 명시적 다크, **아무것도 안 고름(시스템 따름, 기본값)**.
>
> **토큰 이름은 톤이 아니라 역할이다.** `--bg-deep` 이 아니라 `--bg-sunken`, `--text-hi` 가 아니라 `--text-strong`. 톤 기반 이름은 반대 테마에서 뜻을 잃는다.
>
> **`--accent` 는 테마마다 관계가 뒤집힌다.** 다크는 시안 바탕에 어두운 글자, 라이트는 어두운 청록 바탕에 흰 글자다. `--accent-ink` 가 "강조색 위에 얹는 글자색"이라는 역할 이름이라 `Button` 은 바뀌지 않는다.
>
> **`index.html` 의 인라인 스크립트를 지우지 말 것.** 첫 페인트 전에 `data-theme` 을 박지 않으면 새로고침마다 반대 테마가 번쩍인다.
>
> **로그인 · 회원가입은 항상 다크다.** `AuthLayout.module.scss` 가 `@include t.dark-tokens` 로 서브트리를 고정한다. 웨이퍼 노광은 어두운 배경에 다이가 빛나는 것이 개념의 전부라 라이트에서 성립하지 않는다.
>
> **테마를 Redux 에 넣지 말 것.** 진실은 `<html data-theme>` 속성이고 `useSyncExternalStore` 로 구독한다. store 에 복사하면 인라인 스크립트와 진실이 갈린다.

**인증 절** — 승인제를 더한다:

> ### 승인제
>
> 가입하면 `PENDING` 이고 관리자가 승인해야 로그인할 수 있다. **승인 전 로그인·재발급은 403** 이다(`APPROVAL_PENDING` · `SIGNUP_REJECTED`).
>
> `useLogin` 이 이 코드들을 401 과 구분해 처리한다. **섞으면 "이메일 또는 비밀번호가 올바르지 않습니다" 가 떠서 사용자가 비밀번호를 계속 다시 친다.**
>
> **거절은 access token 을 무효화하지 못한다.** 최대 30분간 살아 있어서 `useRejectedGuard` 가 `me.status` 를 보고 이 브라우저에서만 끊는다. 완화책이지 차단이 아니다.

- [ ] **Step 3: 링크가 살아 있는지 확인한다**

Run:
```bash
git grep -n 'docs/api/users.md' -- CLAUDE.md README.md docs
```
README 의 문서 표에 `docs/api/users.md` 행을 더한다.

- [ ] **Step 4: 최종 검증**

Run: `yarn lint && yarn tsc -b && yarn test && yarn build`
Expected: 전부 통과

- [ ] **Step 5: 커밋**

```bash
git add CLAUDE.md README.md docs/api/users.md
git commit -m "docs: widgets 레이어 · 테마 · 승인제 문서화"
```

---

## 검증 요약

전 작업 완료 후:

```bash
yarn lint && yarn tsc -b && yarn test && yarn build
```

수동 확인:

| | |
|---|---|
| 테마 | 토글 → 새로고침해도 유지 · 번쩍임 없음 · 시스템 설정 변경 시 아이콘 갱신 |
| 로그인 화면 | 테마와 무관하게 항상 다크 |
| 헤더 | 홈 ↔ 승인 관리 이동에 재마운트되지 않음 |
| 일반 사용자 | 승인 관리 링크 없음. `/admin/users` 직접 입력 시 홈으로 |
| 승인 관리 | 탭 3개 · 승인 시 목록 갱신 · 자기 자신 거절 비활성 · 빈 상태 |
| 승인 대기 계정 | 로그인 시 "관리자 승인 대기 중입니다" |
| 모바일 | 헤더에서 이름 숨김 · 목록 버튼이 아래로 |
