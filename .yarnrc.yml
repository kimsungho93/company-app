# corepack 을 쓸 수 없는 환경(관리자 권한 필요)이라 릴리스를 프로젝트에 두고 실행한다.
# 이 파일과 .yarn/releases 를 커밋해야 다른 사람도 같은 버전을 쓴다.
yarnPath: .yarn/releases/yarn-4.18.0.cjs

# Yarn 4 기본값은 PnP 다. Vite 플러그인·vitest 와 마찰이 생기는 경우가 있어
# 지금까지와 동일한 node_modules 구조를 유지한다.
nodeLinker: node-modules
